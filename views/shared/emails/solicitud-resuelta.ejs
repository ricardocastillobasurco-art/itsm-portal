<%
const body = `
<div class="header" style="background:linear-gradient(135deg,#10b981,#059669);">
  <h1>🎉 Solicitud Completada</h1>
  <p>Tu solicitud de servicio ha sido resuelta</p>
</div>
<div class="body">
  <div class="field"><div class="field-label">Solicitud</div><div class="field-val">${request.title}</div></div>
  <div class="field"><div class="field-label">Estado</div><div class="field-val" style="color:#059669;font-weight:700;">Completado ✓</div></div>
  <p style="font-size:13px;color:#64748b;margin-top:16px;">Si tienes alguna consulta adicional, por favor abre un nuevo ticket.</p>
  <div class="cta"><a href="${process.env.APP_URL}/solicitudes" style="background:#10b981;">Ver Solicitud</a></div>
</div>`;
%>
<%- include('_base', { body }) %>
