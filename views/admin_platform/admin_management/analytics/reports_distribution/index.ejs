<!DOCTYPE html>
<html lang="es" data-theme="light">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Reportes & Distribución</title>
<script>(function(){const s=localStorage.getItem('dashboardTheme')||'light';document.documentElement.setAttribute('data-theme',s);})();</script>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
<link href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@300;400;500;600;700;800&family=DM+Mono:wght@400;500&display=swap" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.2/css/all.min.css">
<link rel="stylesheet" href="../css/core/style.css">
<style>
/* ══ VARIABLES ══════════════════════════════════════════════ */
:root,[data-theme="light"]{
  --bg:#f0f4f8;--card:#ffffff;--header:#f8fafc;--border:#e2e8f0;
  --text:#1e293b;--muted:#64748b;--primary:#3b82f6;--success:#10b981;
  --warning:#f59e0b;--danger:#ef4444;--info:#06b6d4;--purple:#8b5cf6;
  --hover:#f1f5f9;--input-bg:#f8fafc;--input-ro:#f8fafc;
  --shadow:0 1px 3px rgba(0,0,0,.07);--shadow-md:0 4px 16px rgba(0,0,0,.10);
  --alert-ok-bg:#f0fdf4;--alert-ok-color:#166534;--alert-ok-border:#86efac;
}
[data-theme="dark"]{
  --bg:#0f172a;--card:#1e293b;--header:#162032;--border:#2d3f55;
  --text:#e2e8f0;--muted:#94a3b8;--primary:#60a5fa;--success:#34d399;
  --warning:#fbbf24;--danger:#f87171;--info:#22d3ee;--purple:#a78bfa;
  --hover:#243348;--input-bg:#162032;--input-ro:#162032;
  --shadow:0 1px 3px rgba(0,0,0,.3);--shadow-md:0 4px 16px rgba(0,0,0,.4);
  --alert-ok-bg:#064e3b;--alert-ok-color:#a7f3d0;--alert-ok-border:#34d399;
}
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0;}
body{background:var(--bg);color:var(--text);font-family:'DM Sans',-apple-system,BlinkMacSystemFont,'Segoe UI',sans-serif;font-size:13.5px;transition:background .3s,color .3s;}

/* ══ LAYOUT PRINCIPAL ═══════════════════════════════════════ */
.pg{margin-top:0;padding:20px 24px;display:flex;flex-direction:column;gap:0;max-width:1680px;margin-left:auto;margin-right:auto;}
.pg-header{display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:12px;padding-bottom:18px;}
.pg-title{font-size:21px;font-weight:800;letter-spacing:-.3px;display:flex;align-items:center;gap:10px;}
.pg-title i{color:var(--primary);}
.pg-sub{font-size:12px;color:var(--muted);margin-top:3px;}

/* ══ PESTAÑAS DE MÓDULO ══════════════════════════════════════ */
.mod-nav{display:flex;background:var(--card);border:1px solid var(--border);border-radius:14px 14px 0 0;border-bottom:none;overflow:hidden;box-shadow:var(--shadow);}
.mod-tab{flex:1;padding:14px 20px;border:none;background:transparent;color:var(--muted);cursor:pointer;font-family:'DM Sans',sans-serif;font-size:13.5px;font-weight:700;transition:all .2s;display:flex;align-items:center;justify-content:center;gap:8px;border-bottom:3px solid transparent;letter-spacing:-.1px;}
.mod-tab:not(:last-child){border-right:1px solid var(--border);}
.mod-tab:hover{background:var(--hover);color:var(--text);}
.mod-tab.active{background:var(--header);color:var(--primary);border-bottom-color:var(--primary);}
.mod-tab .tb{font-size:16px;}
.mod-tab .badge{display:inline-flex;align-items:center;justify-content:center;min-width:20px;height:20px;border-radius:10px;font-size:10px;font-weight:800;padding:0 5px;background:var(--border);color:var(--muted);}
.mod-tab.active .badge{background:rgba(59,130,246,.15);color:var(--primary);}

.mod-shell{background:var(--card);border:1px solid var(--border);border-top:none;border-radius:0 0 14px 14px;box-shadow:var(--shadow);overflow:hidden;}
.mod-panel{display:none;}
.mod-panel.active{display:block;}

/* ══ BOTONES COMPARTIDOS ════════════════════════════════════ */
.btn-primary{background:var(--primary);color:#fff;border:none;border-radius:9px;padding:8px 18px;font-size:13px;font-weight:700;cursor:pointer;display:inline-flex;align-items:center;gap:7px;transition:all .2s;white-space:nowrap;font-family:'DM Sans',sans-serif;}
.btn-primary:hover{filter:brightness(1.1);transform:translateY(-1px);}
.btn-primary:disabled{opacity:.55;cursor:not-allowed;transform:none;}
.btn-primary.green{background:var(--success);}
.btn-ghost{background:transparent;color:var(--primary);border:1.5px solid var(--primary);border-radius:9px;padding:7px 14px;font-size:13px;font-weight:600;cursor:pointer;display:inline-flex;align-items:center;gap:7px;transition:all .2s;font-family:'DM Sans',sans-serif;}
.btn-ghost:hover{background:var(--primary);color:#fff;}
.btn-sm{padding:5px 9px;font-size:11.5px;border-radius:7px;border:1px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;display:inline-flex;align-items:center;gap:4px;transition:all .15s;white-space:nowrap;font-family:'DM Sans',sans-serif;}
.btn-sm:hover{border-color:var(--primary);color:var(--primary);}
.btn-sm.danger:hover{border-color:var(--danger);color:var(--danger);}
.btn-preview{padding:5px 12px;border-radius:7px;font-size:12px;font-weight:600;border:1.5px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;transition:all .2s;font-family:'DM Sans',sans-serif;display:inline-flex;align-items:center;gap:6px;}
.btn-preview:hover,.btn-preview.on{border-color:var(--info);color:var(--info);background:rgba(6,182,212,.08);}

/* ══ FORMULARIO ═════════════════════════════════════════════ */
.fc{padding:9px 12px;border:1.5px solid var(--border);border-radius:9px;font-size:13px;font-family:'DM Sans',sans-serif;background:var(--input-bg);color:var(--text);outline:none;width:100%;transition:border-color .2s,box-shadow .2s,background .3s,color .3s;}
.fc::placeholder{color:var(--muted);}
.fc:focus{border-color:var(--primary);box-shadow:0 0 0 3px rgba(59,130,246,.1);}
.fc[readonly]{background:var(--input-ro);color:var(--muted);cursor:default;}
.fc.invalid{border-color:var(--danger);}
textarea.fc{resize:vertical;min-height:88px;line-height:1.65;}
.fl{font-size:10.5px;font-weight:700;letter-spacing:.5px;text-transform:uppercase;color:var(--muted);}
.fg{display:flex;flex-direction:column;gap:5px;margin-bottom:13px;}
.fg:last-child{margin-bottom:0;}
.req{color:var(--danger);}
.grid2{display:grid;grid-template-columns:1fr 1fr;gap:13px;}
@media(max-width:600px){.grid2{grid-template-columns:1fr;}}

/* ══ TARJETA INTERNA ════════════════════════════════════════ */
.icard{background:var(--card);border:1px solid var(--border);border-radius:12px;overflow:hidden;margin-bottom:14px;box-shadow:var(--shadow);}
.icard:last-child{margin-bottom:0;}
.icard-head{padding:12px 18px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;font-weight:700;font-size:13px;color:var(--text);}
.icard-head i{color:var(--primary);margin-right:6px;}
.icard-body{padding:16px 18px;}
.cbadge{display:inline-flex;align-items:center;justify-content:center;min-width:17px;height:17px;border-radius:9px;background:var(--primary);color:#fff;font-size:10px;font-weight:800;padding:0 4px;}
.count-badge{background:var(--input-bg);color:var(--muted);font-size:11px;font-weight:700;padding:2px 9px;border-radius:20px;border:1px solid var(--border);}

/* ══ TAGS (para / cc / bcc) ═════════════════════════════════ */
.tags-wrap{min-height:42px;padding:5px 8px;display:flex;flex-wrap:wrap;gap:5px;align-items:center;cursor:text;border:1.5px solid var(--border);border-radius:9px;background:var(--input-bg);transition:border-color .2s,box-shadow .2s,background .3s;}
.tags-wrap:focus-within{border-color:var(--primary);box-shadow:0 0 0 3px rgba(59,130,246,.1);}
.tag{display:inline-flex;align-items:center;gap:5px;padding:3px 9px;border-radius:20px;font-size:11.5px;font-weight:600;background:rgba(59,130,246,.1);color:var(--primary);animation:tagPop .15s ease;max-width:220px;}
.tag.ext{background:rgba(6,182,212,.1);color:var(--info);}
.tag.dist{background:rgba(245,158,11,.1);color:var(--warning);}
.tag span{overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.tag-x{background:none;border:none;cursor:pointer;color:inherit;opacity:.6;padding:0;font-size:14px;line-height:1;transition:opacity .15s;flex-shrink:0;}
.tag-x:hover{opacity:1;}
@keyframes tagPop{from{opacity:0;transform:scale(.82);}to{opacity:1;transform:scale(1);}}
.tags-input{border:none;outline:none;background:transparent;font-size:13px;font-family:'DM Sans',sans-serif;color:var(--text);min-width:130px;flex:1;padding:2px 4px;}
.tags-input::placeholder{color:var(--muted);}

/* Autocomplete */
.ac-wrap{position:relative;}
.ac-drop{display:none;position:absolute;top:calc(100% + 4px);left:0;right:0;z-index:300;background:var(--card);border:1px solid var(--border);border-radius:10px;box-shadow:var(--shadow-md);max-height:190px;overflow-y:auto;}
.ac-item{padding:8px 12px;cursor:pointer;font-size:12.5px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:9px;transition:background .1s;}
.ac-item:last-child{border-bottom:none;}
.ac-item:hover{background:var(--hover);}
.ac-av{width:28px;height:28px;border-radius:50%;flex-shrink:0;background:rgba(59,130,246,.1);color:var(--primary);display:flex;align-items:center;justify-content:center;font-size:10px;font-weight:800;}
.ac-name{font-weight:600;font-size:12.5px;}
.ac-email{font-size:11px;color:var(--muted);font-family:'DM Mono',monospace;}
.ac-dept{font-size:10px;color:var(--muted);margin-left:auto;white-space:nowrap;}

/* CC / BCC */
.cc-row{display:flex;gap:7px;margin-bottom:13px;}
.btn-cc{padding:4px 11px;border-radius:7px;font-size:12px;font-weight:600;border:1.5px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;transition:all .2s;font-family:'DM Sans',sans-serif;}
.btn-cc:hover{border-color:var(--primary);color:var(--primary);}
.btn-cc.on{border-color:var(--primary);color:var(--primary);background:rgba(59,130,246,.08);}

/* Chips distribución */
.dc-row{display:flex;flex-wrap:wrap;gap:7px;}
.dc{padding:4px 11px;border-radius:20px;font-size:12px;font-weight:600;border:1.5px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;transition:all .2s;font-family:'DM Sans',sans-serif;display:flex;align-items:center;gap:5px;}
.dc:hover,.dc.active{border-color:var(--warning);color:var(--warning);background:rgba(245,158,11,.08);}

/* Templates */
.tpl-row{display:flex;flex-wrap:wrap;gap:6px;margin-top:6px;}
.tpl-btn{padding:4px 11px;border-radius:7px;font-size:11.5px;font-weight:500;border:1.5px solid var(--border);background:var(--header);color:var(--text);cursor:pointer;transition:all .2s;font-family:'DM Sans',sans-serif;}
.tpl-btn:hover{border-color:var(--primary);color:var(--primary);}

/* File tabs */
.file-tabs{display:flex;border-bottom:2px solid var(--border);margin-bottom:14px;}
.ft-tab{padding:8px 17px;font-size:13px;font-weight:600;border:none;background:transparent;color:var(--muted);cursor:pointer;border-bottom:2.5px solid transparent;margin-bottom:-2px;transition:all .2s;font-family:'DM Sans',sans-serif;display:flex;align-items:center;gap:7px;}
.ft-tab:hover{color:var(--text);}
.ft-tab.active{color:var(--primary);border-bottom-color:var(--primary);}

/* Drop zone */
.drop-zone{border:2px dashed var(--border);border-radius:10px;padding:28px 18px;text-align:center;cursor:pointer;background:var(--header);position:relative;transition:all .25s;}
.drop-zone:hover,.drop-zone.drag-over{border-color:var(--primary);background:rgba(59,130,246,.04);}
.drop-zone input[type=file]{position:absolute;inset:0;opacity:0;cursor:pointer;width:100%;height:100%;}
.dz-icon{font-size:30px;color:var(--primary);opacity:.4;margin-bottom:7px;display:block;transition:transform .2s;}
.drop-zone:hover .dz-icon{transform:translateY(-4px);opacity:.65;}
.dz-title{font-weight:700;font-size:13.5px;margin-bottom:3px;}
.dz-sub{font-size:11.5px;color:var(--muted);}
.dz-types{display:flex;gap:5px;justify-content:center;flex-wrap:wrap;margin-top:8px;}
.dz-type{padding:2px 7px;border-radius:5px;font-size:10px;font-weight:700;background:var(--card);border:1px solid var(--border);color:var(--muted);}

/* Reportes app */
.rep-list{display:flex;flex-direction:column;gap:7px;}
.rep-item{display:flex;align-items:center;gap:10px;padding:10px 13px;border-radius:9px;border:1.5px solid var(--border);background:var(--header);cursor:pointer;transition:all .2s;position:relative;overflow:hidden;}
.rep-item:hover{border-color:var(--primary);background:rgba(59,130,246,.04);}
.rep-item.sel{border-color:var(--primary);background:rgba(59,130,246,.07);}
.rep-check{width:17px;height:17px;border-radius:50%;background:var(--primary);color:#fff;display:flex;align-items:center;justify-content:center;font-size:10px;flex-shrink:0;opacity:0;transition:opacity .15s;}
.rep-item.sel .rep-check{opacity:1;}
.rep-name{font-size:13px;font-weight:600;}
.rep-meta{font-size:11px;color:var(--muted);}
.rep-badge{margin-left:auto;padding:2px 7px;border-radius:5px;font-size:10px;font-weight:700;white-space:nowrap;flex-shrink:0;}
.rb-pdf{background:rgba(239,68,68,.1);color:#ef4444;}
.rb-xls{background:rgba(16,185,129,.1);color:#10b981;}
.rep-gen-overlay{display:none;position:absolute;inset:0;border-radius:9px;background:rgba(59,130,246,.07);align-items:center;justify-content:center;gap:7px;font-size:12px;font-weight:700;color:var(--primary);}
.rep-item.generating .rep-gen-overlay{display:flex;}
.rep-item.generating{pointer-events:none;}

/* Adjuntos */
.att-list{display:flex;flex-direction:column;gap:7px;margin-top:11px;}
.att-item{display:flex;align-items:center;gap:9px;padding:9px 12px;border-radius:9px;border:1px solid var(--border);background:var(--header);animation:tagPop .2s ease;}
.att-icon{font-size:19px;flex-shrink:0;}
.att-pdf{color:#ef4444;}.att-word{color:#3b82f6;}.att-excel{color:#10b981;}.att-ppt{color:#f97316;}.att-img{color:#8b5cf6;}.att-other{color:var(--muted);}
.att-info{flex:1;min-width:0;}
.att-name{font-size:12.5px;font-weight:600;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.att-size{font-size:11px;color:var(--muted);}
.att-src{font-size:10px;font-weight:700;padding:1px 6px;border-radius:4px;margin-left:4px;}
.att-src.app{background:rgba(59,130,246,.1);color:var(--primary);}
.att-src.upload{background:var(--header);border:1px solid var(--border);color:var(--muted);}
.att-rm{background:none;border:none;color:var(--muted);cursor:pointer;padding:4px;border-radius:6px;display:flex;align-items:center;transition:all .15s;}
.att-rm:hover{background:rgba(239,68,68,.1);color:var(--danger);}

/* Preview correo */
.ep-wrap{border:1.5px solid var(--border);border-radius:11px;overflow:hidden;margin-bottom:14px;animation:slideD .22s ease;}
@keyframes slideD{from{opacity:0;transform:translateY(-8px);}to{opacity:1;transform:translateY(0);}}
.ep-bar{padding:8px 12px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;gap:5px;}
.ep-dot{width:11px;height:11px;border-radius:50%;}
.ep-lbl{font-size:11px;color:var(--muted);margin-left:7px;font-family:'DM Mono',monospace;}
.ep-body{padding:14px 16px;background:var(--card);}
.ep-row{display:flex;gap:8px;padding:5px 0;border-bottom:1px solid var(--border);font-size:12.5px;}
.ep-row:last-of-type{border-bottom:none;}
.ep-k{font-weight:700;color:var(--muted);min-width:58px;font-size:10.5px;text-transform:uppercase;letter-spacing:.4px;padding-top:1px;}
.ep-v{color:var(--text);flex:1;word-break:break-word;}
.ep-content{margin-top:10px;padding:11px;border-radius:8px;background:var(--header);border:1px solid var(--border);white-space:pre-wrap;line-height:1.65;font-size:12.5px;color:var(--muted);}
.ep-atts{margin-top:9px;display:flex;flex-wrap:wrap;gap:6px;}
.ep-att-chip{display:flex;align-items:center;gap:5px;padding:3px 9px;border-radius:7px;background:var(--header);border:1px solid var(--border);font-size:11.5px;font-weight:600;}

/* Status envío */
.sr-status{display:none;padding:11px 14px;border-radius:9px;margin-top:10px;align-items:center;gap:10px;font-size:13px;font-weight:600;}
.sr-status.show{display:flex;}
.sr-status.sending{background:rgba(6,182,212,.1);color:var(--info);}
.sr-status.ok{background:var(--alert-ok-bg);color:var(--alert-ok-color);border:1px solid var(--alert-ok-border);}
.sr-status.err{background:rgba(239,68,68,.1);color:var(--danger);}
.mini-prog{height:3px;background:var(--border);border-radius:2px;margin-top:7px;overflow:hidden;}
.mini-prog-fill{height:100%;border-radius:2px;background:var(--primary);width:0%;transition:width .3s;}
@keyframes prog-ind{0%{transform:translateX(-100%);}100%{transform:translateX(300%);}}
.mini-prog-fill.ind{width:30%;animation:prog-ind 1.1s infinite;}

/* Historial */
.hist-search{position:relative;margin-bottom:10px;}
.hist-search i{position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--muted);font-size:12px;pointer-events:none;}
.hist-search input{width:100%;padding:7px 10px 7px 30px;border:1.5px solid var(--border);border-radius:9px;font-size:12.5px;background:var(--input-bg);color:var(--text);outline:none;font-family:'DM Sans',sans-serif;transition:border-color .2s;}
.hist-search input:focus{border-color:var(--primary);}
.hist-search input::placeholder{color:var(--muted);}
.hi{padding:10px 12px;border-radius:10px;border:1px solid var(--border);background:var(--header);cursor:pointer;transition:all .2s;margin-bottom:7px;}
.hi:hover{border-color:var(--primary);transform:translateX(2px);}
.hi-top{display:flex;justify-content:space-between;gap:6px;margin-bottom:3px;}
.hi-subj{font-size:12.5px;font-weight:700;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;flex:1;}
.hi-time{font-size:10px;color:var(--muted);white-space:nowrap;font-family:'DM Mono',monospace;}
.hi-to{font-size:11px;color:var(--muted);overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.hi-badges{display:flex;gap:5px;margin-top:5px;flex-wrap:wrap;}
.hb{display:inline-flex;align-items:center;gap:3px;padding:2px 7px;border-radius:20px;font-size:10px;font-weight:700;}
.hb-ok{background:var(--alert-ok-bg);color:var(--alert-ok-color);border:1px solid var(--alert-ok-border);}
.hb-err{background:rgba(239,68,68,.1);color:var(--danger);}
.hb-att{background:var(--header);border:1px solid var(--border);color:var(--muted);}
.hist-empty{text-align:center;padding:28px 16px;color:var(--muted);font-size:12.5px;}
.hist-empty i{font-size:26px;display:block;margin-bottom:7px;opacity:.3;}

/* Confirm rows (modal envío) */
.confirm-row{padding:10px 13px;background:var(--header);border-radius:9px;border:1px solid var(--border);margin-bottom:9px;}
.confirm-key{font-size:10px;font-weight:700;text-transform:uppercase;letter-spacing:.4px;color:var(--muted);margin-bottom:3px;}
.confirm-val{font-size:13px;font-weight:600;color:var(--text);white-space:pre-wrap;word-break:break-all;}

/* ══ LISTAS — layout 2 col ══════════════════════════════════ */
.rl-layout{display:grid;grid-template-columns:290px 1fr;min-height:580px;}
@media(max-width:900px){.rl-layout{grid-template-columns:1fr;}}
.ls-panel{border-right:1px solid var(--border);display:flex;flex-direction:column;}
.ls-head{padding:12px 16px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;font-weight:700;font-size:13px;}
.ls-head i{color:var(--primary);margin-right:6px;}
.ls-body{flex:1;overflow-y:auto;max-height:640px;}
.list-item{display:flex;align-items:center;gap:11px;padding:11px 14px;cursor:pointer;border-bottom:1px solid var(--border);transition:background .15s;position:relative;}
.list-item:last-child{border-bottom:none;}
.list-item:hover{background:var(--hover);}
.list-item.active{background:rgba(59,130,246,.07);border-left:3px solid var(--primary);}
.list-dot{width:10px;height:10px;border-radius:50%;flex-shrink:0;}
.list-name{font-weight:600;font-size:13px;flex:1;}
.list-count{font-size:11px;color:var(--muted);background:var(--input-bg);border:1px solid var(--border);padding:1px 8px;border-radius:20px;font-weight:600;}
.list-actions{display:none;gap:4px;}
.list-item:hover .list-actions{display:flex;}
.list-edit-btn,.list-del-btn{background:none;border:none;padding:3px 6px;border-radius:5px;cursor:pointer;font-size:13px;transition:all .15s;}
.list-edit-btn{color:var(--muted);}.list-edit-btn:hover{color:var(--primary);background:rgba(59,130,246,.08);}
.list-del-btn{color:var(--muted);}.list-del-btn:hover{color:var(--danger);background:rgba(239,68,68,.08);}
.cp-panel{display:flex;flex-direction:column;}
.cp-head{padding:12px 16px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;font-weight:700;font-size:13px;}
.cp-toolbar{padding:10px 14px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;gap:8px;flex-wrap:wrap;}
.search-wrap{position:relative;flex:1;min-width:160px;}
.search-wrap i{position:absolute;left:10px;top:50%;transform:translateY(-50%);color:var(--muted);font-size:13px;pointer-events:none;}
.search-inp{width:100%;padding:7px 12px 7px 32px;border:1px solid var(--border);border-radius:9px;font-size:12.5px;background:var(--input-bg);color:var(--text);outline:none;transition:border-color .2s,background .3s;}
.search-inp:focus{border-color:var(--primary);box-shadow:0 0 0 3px rgba(59,130,246,.1);}
.search-inp::placeholder{color:var(--muted);}

/* Tabla contactos */
.tbl-scroll{overflow-x:auto;overflow-y:auto;max-height:560px;}
.tbl{width:100%;border-collapse:collapse;font-size:13px;}
.tbl thead th{font-size:10px;text-transform:uppercase;letter-spacing:.4px;color:var(--muted);font-weight:700;padding:10px 14px;background:var(--header);border-bottom:1px solid var(--border);white-space:nowrap;position:sticky;top:0;z-index:5;}
.tbl tbody td{padding:10px 14px;border-bottom:1px solid var(--border);color:var(--text);vertical-align:middle;}
.tbl tbody tr:hover td{background:var(--hover);}
.tbl tbody tr:last-child td{border-bottom:none;}
.pill{display:inline-block;padding:2px 9px;border-radius:20px;font-size:11px;font-weight:600;white-space:nowrap;}
.p-active{background:rgba(16,185,129,.12);color:#10b981;}
.p-inactive{background:rgba(239,68,68,.1);color:#ef4444;}
.list-chip{display:inline-block;padding:2px 9px;border-radius:20px;font-size:11px;font-weight:600;margin:2px;border:1px solid;}

/* Color picker listas */
.color-opts{display:flex;gap:7px;flex-wrap:wrap;}
.color-opt{width:24px;height:24px;border-radius:50%;cursor:pointer;border:2.5px solid transparent;transition:transform .15px;}
.color-opt:hover{transform:scale(1.15);}
.color-opt.sel{border-color:var(--text);transform:scale(1.15);}
.list-chk-grid{display:flex;flex-direction:column;gap:7px;max-height:200px;overflow-y:auto;padding:2px;}
.list-chk-row{display:flex;align-items:center;gap:9px;padding:8px 11px;border:1.5px solid var(--border);border-radius:9px;cursor:pointer;transition:all .2s;}
.list-chk-row:hover{border-color:var(--primary);background:rgba(59,130,246,.04);}
.list-chk-row.checked{border-color:var(--primary);background:rgba(59,130,246,.07);}
.list-chk-row input{display:none;}
.list-chk-dot{width:10px;height:10px;border-radius:50%;flex-shrink:0;}
.list-chk-name{flex:1;font-weight:600;font-size:13px;}
.list-chk-icon{width:18px;height:18px;border-radius:50%;background:var(--primary);color:#fff;display:flex;align-items:center;justify-content:center;font-size:10px;opacity:0;transition:opacity .15s;}
.list-chk-row.checked .list-chk-icon{opacity:1;}

/* Modal */
.modal-bg{display:none;position:fixed;inset:0;background:rgba(0,0,0,.5);z-index:1050;align-items:center;justify-content:center;backdrop-filter:blur(2px);}
.modal-bg.open{display:flex;}
.modal-box{background:var(--card);border:1px solid var(--border);border-radius:16px;width:100%;max-width:500px;box-shadow:0 20px 60px rgba(0,0,0,.25);animation:mIn .2s ease;overflow:hidden;}
.modal-box.wide{max-width:580px;}
@keyframes mIn{from{opacity:0;transform:scale(.95);}to{opacity:1;transform:scale(1);}}
.modal-head{padding:16px 20px;background:var(--header);border-bottom:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;font-weight:700;font-size:15px;}
.modal-body-p{padding:20px 22px;}
.modal-foot{padding:13px 22px;border-top:1px solid var(--border);display:flex;justify-content:flex-end;gap:8px;background:var(--header);}
.btn-mcancel{background:transparent;border:1px solid var(--border);color:var(--muted);border-radius:9px;padding:7px 16px;font-size:13px;cursor:pointer;transition:all .2s;}
.btn-mcancel:hover{border-color:var(--text);color:var(--text);}
.btn-mclose{width:28px;height:28px;border-radius:7px;border:1px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;transition:all .2s;}
.btn-mclose:hover{background:rgba(239,68,68,.1);color:var(--danger);border-color:var(--danger);}

/* Bootstrap modal dark */
[data-theme="dark"] .modal-content{background:var(--card)!important;border-color:var(--border)!important;color:var(--text)!important;}
[data-theme="dark"] .modal-body,[data-theme="dark"] .modal-footer,[data-theme="dark"] .modal-header{background:var(--card)!important;border-color:var(--border)!important;}

/* Toast */
.toast-wrap{position:fixed;bottom:22px;right:22px;z-index:9999;display:flex;flex-direction:column;gap:7px;}
.toast-item{background:var(--card);border:1px solid var(--border);color:var(--text);border-radius:11px;padding:11px 16px;font-size:13px;font-weight:500;box-shadow:var(--shadow-md);display:flex;align-items:center;gap:9px;min-width:260px;animation:tIn .22s ease;}
.toast-item.ok{border-left:4px solid var(--success);}
.toast-item.err{border-left:4px solid var(--danger);}
.toast-item.inf{border-left:4px solid var(--info);}
@keyframes tIn{from{opacity:0;transform:translateX(22px);}to{opacity:1;transform:translateX(0);}}

/* Misc */
@keyframes spin{to{transform:rotate(360deg);}}
.spin-anim{animation:spin .7s linear infinite;}
.mini-spin{display:inline-block;width:14px;height:14px;border:2px solid var(--border);border-top-color:var(--primary);border-radius:50%;animation:spin .65s linear infinite;}
.empty-state{text-align:center;padding:42px 20px;color:var(--muted);}
.empty-state i{font-size:32px;display:block;margin-bottom:10px;opacity:.3;}
*::-webkit-scrollbar{width:5px;height:5px;}
*::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px;}
*::-webkit-scrollbar-track{background:transparent;}
</style>
</head>
<body>
<style>
.itsm-topbar{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 24px;background:var(--card,#fff);border-bottom:1px solid var(--border,#e2e8f0);position:sticky;top:0;z-index:999;}
.itsm-back{display:inline-flex;align-items:center;gap:6px;font-size:12px;font-weight:600;color:var(--muted,#64748b);background:var(--bg,#f0f4f8);border:1px solid var(--border,#e2e8f0);border-radius:20px;padding:5px 12px;text-decoration:none;transition:color .2s;}
.itsm-back:hover{color:var(--primary,#3b82f6);}
.itsm-breadcrumb{display:flex;align-items:center;gap:6px;font-size:12px;color:var(--muted,#64748b);}
.itsm-breadcrumb a{color:var(--muted,#64748b);text-decoration:none;font-weight:500;transition:color .2s;}
.itsm-breadcrumb a:hover{color:var(--primary,#3b82f6);}
.itsm-breadcrumb .sep{opacity:.4;}
.itsm-breadcrumb .current{color:var(--text,#1e293b);font-weight:700;}
</style>
<div class="itsm-topbar">
  <div style="display:flex;align-items:center;gap:12px;">
    <a href="/administracion" class="itsm-back"><i class="bi bi-arrow-left"></i> Admin</a>
    <div class="itsm-breadcrumb">
      <a href="/administracion"><i class="bi bi-shield-lock-fill"></i> Admin</a>
      <span class="sep">/</span>
      <span>Reportería</span>
      <span class="sep">/</span>
      <span class="current"><i class="bi bi-send-fill"></i> Reportes &amp; Distribución</span>
    </div>
  </div>
  <button onclick="toggleTheme()" style="display:flex;align-items:center;gap:6px;background:none;border:none;cursor:pointer;padding:4px 8px;border-radius:8px;">
    <div id="themeTrack" style="width:32px;height:18px;border-radius:9px;background:#cbd5e1;position:relative;transition:background .3s;">
      <div id="themeKnob" style="width:14px;height:14px;border-radius:50%;background:#fff;position:absolute;top:2px;left:2px;transition:transform .3s;"></div>
    </div>
    <i id="toggleIcon" class="bi bi-moon-stars-fill" style="font-size:14px;color:var(--text-muted);"></i>
    <span id="toggleLabel" style="font-size:12px;color:var(--text-muted);font-weight:500;">Oscuro</span>
  </button>
</div>

<script>
/* ─── Dark Mode — init inmediato (sin flash) ────────── */
(function () {
    const saved = localStorage.getItem('dashboardTheme') || 'light';
    document.documentElement.setAttribute('data-theme', saved);
})();

function toggleTheme() {
    const html    = document.documentElement;
    const current = html.getAttribute('data-theme');
    const next    = current === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', next);
    localStorage.setItem('dashboardTheme', next);
    _syncToggleUI(next);
    if (typeof updateChartsTheme === 'function') updateChartsTheme(next);
}

function _syncToggleUI(theme) {
    const dark=theme==='dark';
    const icon=document.getElementById('toggleIcon'),label=document.getElementById('toggleLabel'),track=document.getElementById('themeTrack'),knob=document.getElementById('themeKnob');
    if(icon)icon.className=dark?'bi bi-sun-fill':'bi bi-moon-stars-fill';
    if(label)label.textContent=dark?'Claro':'Oscuro';
    if(track)track.style.background=dark?'#3b82f6':'#cbd5e1';
    if(knob)knob.style.transform=dark?'translateX(15px)':'translateX(0)';
}

document.addEventListener('DOMContentLoaded', function () {
    const saved = localStorage.getItem('dashboardTheme') || 'light';
    _syncToggleUI(saved);
});
</script>
<div class="toast-wrap" id="toastWrap"></div>

<div class="pg">

  <!-- ══ HEADER ══════════════════════════════════════════════ -->
  <div class="pg-header">
    <div>
      <div class="pg-title"><i class="bi bi-send-fill"></i> Reportes &amp; Distribución</div>
      <div class="pg-sub">Envío de reportes por correo · Gestión de listas de destinatarios</div>
    </div>
    <div id="pgActions" style="display:flex;gap:8px;align-items:center;"></div>
  </div>

  <!-- ══ PESTAÑAS ═════════════════════════════════════════════ -->
  <nav class="mod-nav">
    <button class="mod-tab active" id="mTabEnvio" onclick="switchMod('envio')">
      <i class="bi bi-send-fill tb"></i> Envío de Reportes
      <span class="badge" id="mBadgeEnvio">0</span>
    </button>
    <button class="mod-tab" id="mTabListas" onclick="switchMod('listas')">
      <i class="bi bi-envelope-paper-fill tb"></i> Listas de Distribución
      <span class="badge" id="mBadgeListas">0</span>
    </button>
  </nav>

  <!-- ══ SHELL ════════════════════════════════════════════════ -->
  <div class="mod-shell">

    <!-- ══════════════════════════════
         PANEL ENVÍO DE REPORTES
         (código original intacto)
    ══════════════════════════════ -->
    <div class="mod-panel active" id="panelEnvio">
      <div style="display:grid;grid-template-columns:1fr 320px;gap:0;">

        <!-- Columna principal -->
        <div style="padding:20px;border-right:1px solid var(--border);display:flex;flex-direction:column;gap:0;">

          <!-- Header envío -->
          <div style="display:flex;align-items:flex-start;justify-content:space-between;gap:12px;flex-wrap:wrap;margin-bottom:14px;">
            <div>
              <div style="font-size:16px;font-weight:800;letter-spacing:-.2px;display:flex;align-items:center;gap:8px;"><i class="bi bi-send-fill" style="color:var(--primary);font-size:15px;"></i> Envío de Reportes</div>
              <div style="font-size:12px;color:var(--muted);margin-top:2px;">Genera reportes de la app o adjunta archivos y envíalos por correo</div>
            </div>
            <button class="btn-preview" id="btnPreview" onclick="togglePreview()"><i class="bi bi-eye"></i> Vista previa</button>
          </div>

          <!-- Vista previa -->
          <div id="previewPanel" style="display:none;">
            <div class="ep-wrap">
              <div class="ep-bar"><div class="ep-dot" style="background:#ef4444;"></div><div class="ep-dot" style="background:#f59e0b;"></div><div class="ep-dot" style="background:#10b981;"></div><span class="ep-lbl">Vista previa del correo</span></div>
              <div class="ep-body">
                <div class="ep-row"><span class="ep-k">Para</span><span class="ep-v" id="pvTo">—</span></div>
                <div class="ep-row" id="pvCcRow" style="display:none;"><span class="ep-k">CC</span><span class="ep-v" id="pvCc"></span></div>
                <div class="ep-row" id="pvBccRow" style="display:none;"><span class="ep-k">CCO</span><span class="ep-v" id="pvBcc"></span></div>
                <div class="ep-row"><span class="ep-k">Asunto</span><span class="ep-v" id="pvSubject" style="font-weight:700;">—</span></div>
                <div class="ep-content" id="pvBody">—</div>
                <div class="ep-atts" id="pvAtts"></div>
              </div>
            </div>
          </div>

          <!-- Destinatarios -->
          <div class="icard">
            <div class="icard-head"><span><i class="bi bi-people-fill"></i>Destinatarios</span><span class="cbadge" id="rcCount">0</span></div>
            <div class="icard-body">
              <div class="fg">
                <label class="fl">Para <span class="req">*</span></label>
                <div class="ac-wrap">
                  <div class="tags-wrap" id="toWrap" onclick="focusTI('toInput')">
                    <input class="tags-input" id="toInput" placeholder="nombre o correo, Enter para añadir…" oninput="acSearch(this,'toAc','to')" onkeydown="tagKD(event,'to')" autocomplete="off">
                  </div>
                  <div class="ac-drop" id="toAc"></div>
                </div>
                <small style="font-size:11px;color:var(--muted);">Pulsa <kbd style="background:var(--header);border:1px solid var(--border);border-radius:4px;padding:1px 5px;font-size:10px;">Enter</kbd> o coma para añadir. Autocompletado desde empleados activos.</small>
              </div>
              <div class="cc-row">
                <button class="btn-cc" id="ccToggle" onclick="toggleCC('cc')">+ CC</button>
                <button class="btn-cc" id="bccToggle" onclick="toggleCC('bcc')">+ CCO</button>
              </div>
              <div class="fg" id="ccRow" style="display:none;">
                <label class="fl">CC</label>
                <div class="ac-wrap"><div class="tags-wrap" id="ccWrap" onclick="focusTI('ccInput')"><input class="tags-input" id="ccInput" placeholder="correo en copia…" oninput="acSearch(this,'ccAc','cc')" onkeydown="tagKD(event,'cc')" autocomplete="off"></div><div class="ac-drop" id="ccAc"></div></div>
              </div>
              <div class="fg" id="bccRow" style="display:none;">
                <label class="fl">CCO (Copia Oculta)</label>
                <div class="ac-wrap"><div class="tags-wrap" id="bccWrap" onclick="focusTI('bccInput')"><input class="tags-input" id="bccInput" placeholder="correo en copia oculta…" oninput="acSearch(this,'bccAc','bcc')" onkeydown="tagKD(event,'bcc')" autocomplete="off"></div><div class="ac-drop" id="bccAc"></div></div>
              </div>
              <!-- Chips de distribución: listas de la BD + estáticas -->
              <div class="fg" style="margin-bottom:0;">
                <label class="fl">Listas de distribución</label>
                <div class="dc-row" id="dcRow"><span style="font-size:12px;color:var(--muted);">Cargando listas…</span></div>
              </div>
            </div>
          </div>

          <!-- Contenido -->
          <div class="icard">
            <div class="icard-head"><i class="bi bi-pencil-square"></i>Contenido del correo</div>
            <div class="icard-body">
              <div class="fg">
                <label class="fl">Asunto <span class="req">*</span></label>
                <input class="fc" id="subject" placeholder="Asunto del correo…" oninput="updatePreview()">
                <div class="tpl-row" id="tplRow"></div>
              </div>
              <div class="fg" style="margin-bottom:0;">
                <label class="fl">Mensaje</label>
                <textarea class="fc" id="body" rows="5" placeholder="Cuerpo del correo…" oninput="updatePreview()"></textarea>
              </div>
            </div>
          </div>

          <!-- Adjuntos -->
          <div class="icard">
            <div class="icard-head"><span><i class="bi bi-paperclip"></i>Adjuntos</span><span class="cbadge" id="attCount">0</span></div>
            <div class="icard-body">
              <div class="file-tabs">
                <button class="ft-tab active" id="ftUpload" onclick="switchFT('upload')"><i class="bi bi-upload"></i> Subir archivo</button>
                <button class="ft-tab" id="ftApp" onclick="switchFT('app')"><i class="bi bi-grid-3x3-gap-fill"></i> Reportes de la app</button>
              </div>
              <div id="panelUpload">
                <div class="drop-zone" id="dropZone">
                  <input type="file" id="fileInput" multiple accept=".pdf,.doc,.docx,.xls,.xlsx,.ppt,.pptx,.png,.jpg,.jpeg,.txt,.csv" onchange="handleFiles(this.files)">
                  <i class="bi bi-cloud-arrow-up dz-icon"></i>
                  <div class="dz-title">Arrastra archivos aquí o haz clic</div>
                  <div class="dz-sub">Máximo 25 MB por archivo</div>
                  <div class="dz-types"><span class="dz-type">PDF</span><span class="dz-type">Word</span><span class="dz-type">Excel</span><span class="dz-type">PPT</span><span class="dz-type">Imagen</span><span class="dz-type">CSV</span></div>
                </div>
              </div>
              <div id="panelApp" style="display:none;">
                <div id="repListWrap"><div style="text-align:center;padding:28px;color:var(--muted);font-size:12.5px;"><span class="mini-spin"></span> Cargando reportes…</div></div>
              </div>
              <div id="attList" class="att-list"></div>
            </div>
          </div>

          <!-- Acciones -->
          <div style="display:flex;justify-content:flex-end;gap:9px;align-items:center;flex-wrap:wrap;margin-bottom:14px;margin-top:4px;">
            <button class="btn-sm" onclick="clearForm()"><i class="bi bi-trash3"></i> Limpiar</button>
            <button class="btn-preview" onclick="togglePreview()"><i class="bi bi-eye"></i> Vista previa</button>
            <button class="btn-primary green" id="btnSend" onclick="sendEmail()"><i class="bi bi-send-fill"></i> Enviar reporte</button>
          </div>
          <div class="sr-status" id="srStatus"></div>
          <div class="mini-prog" id="miniProg" style="display:none;"><div class="mini-prog-fill ind"></div></div>
        </div>

        <!-- Historial lateral -->
        <div style="padding:16px;background:var(--header);display:flex;flex-direction:column;gap:10px;">
          <div style="font-weight:800;font-size:13px;display:flex;align-items:center;gap:7px;"><i class="bi bi-clock-history" style="color:var(--primary);"></i> Historial de envíos</div>
          <div class="hist-search"><i class="bi bi-search"></i><input id="histSearch" placeholder="Buscar en historial…" oninput="filterHist()"></div>
          <div id="histList" style="overflow-y:auto;max-height:580px;"></div>
        </div>
      </div>
    </div><!-- /panelEnvio -->

    <!-- ══════════════════════════════
         PANEL LISTAS DE DISTRIBUCIÓN
         (código original intacto)
    ══════════════════════════════ -->
    <div class="mod-panel" id="panelListas">
      <div class="rl-layout">

        <!-- Sidebar -->
        <div class="ls-panel">
          <div class="ls-head"><span><i class="bi bi-collection-fill"></i>Listas</span><button class="btn-sm" onclick="openModalNewList()" title="Nueva lista"><i class="bi bi-plus"></i></button></div>
          <div class="ls-body" id="listsSidebar"><div style="text-align:center;padding:28px;color:var(--muted);"><span class="mini-spin"></span></div></div>
        </div>

        <!-- Contactos -->
        <div class="cp-panel">
          <div class="cp-head"><span><i class="bi bi-people-fill"></i><span id="panelTitle">Todos los destinatarios</span> <span class="count-badge ms-2" id="contactCount">0</span></span></div>
          <div class="cp-toolbar">
            <div class="search-wrap"><i class="bi bi-search"></i><input type="text" class="search-inp" id="searchContacts" placeholder="Buscar por nombre o email…" oninput="filterContacts()"></div>
            <button class="btn-sm" onclick="loadContacts()" title="Recargar"><i class="bi bi-arrow-clockwise"></i></button>
            <button class="btn-sm" onclick="exportContacts()"><i class="bi bi-download"></i> CSV</button>
          </div>
          <div class="tbl-scroll">
            <table class="tbl">
              <thead><tr><th>Nombre</th><th>Email</th><th>Listas</th><th>Estado</th><th>Acciones</th></tr></thead>
              <tbody id="contactsTbody"><tr><td colspan="5" class="empty-state"><span class="mini-spin"></span><p style="margin:8px 0 0;font-size:12px;">Cargando destinatarios…</p></td></tr></tbody>
            </table>
          </div>
          <div style="padding:10px 16px;border-top:1px solid var(--border);background:var(--header);font-size:12px;color:var(--muted);"><span id="contactsFooter">—</span></div>
        </div>
      </div>
    </div><!-- /panelListas -->

  </div><!-- /mod-shell -->
</div><!-- /.pg -->

<!-- ══ MODALES LISTAS (originales) ══════════════════════════ -->
<div class="modal-bg" id="modalList">
  <div class="modal-box">
    <div class="modal-head"><span id="modalListTitle" style="display:flex;align-items:center;gap:8px;"><i class="bi bi-collection-fill" style="color:var(--primary);"></i> Nueva lista</span><button class="btn-mclose" onclick="closeModal('modalList')"><i class="bi bi-x"></i></button></div>
    <div class="modal-body-p">
      <input type="hidden" id="listId">
      <div class="fg"><label class="fl">Nombre <span class="req">*</span></label><input class="fc" id="listName" placeholder="Ej: Técnicos de campo"></div>
      <div class="fg"><label class="fl">Descripción</label><input class="fc" id="listDesc" placeholder="Opcional…"></div>
      <div class="fg"><label class="fl">Color de identificación</label><div class="color-opts" id="colorOpts"></div></div>
    </div>
    <div class="modal-foot"><button class="btn-mcancel" onclick="closeModal('modalList')">Cancelar</button><button class="btn-primary" id="btnSaveList" onclick="saveList()"><i class="bi bi-check-lg"></i> Guardar lista</button></div>
  </div>
</div>

<div class="modal-bg" id="modalContact">
  <div class="modal-box wide">
    <div class="modal-head"><span id="modalContactTitle" style="display:flex;align-items:center;gap:8px;"><i class="bi bi-person-plus-fill" style="color:var(--success);"></i> Añadir destinatario</span><button class="btn-mclose" onclick="closeModal('modalContact')"><i class="bi bi-x"></i></button></div>
    <div class="modal-body-p">
      <input type="hidden" id="contactId">
      <div class="grid2" style="margin-bottom:13px;">
        <div class="fg" style="margin-bottom:0;"><label class="fl">Nombre completo <span class="req">*</span></label><input class="fc" id="contactName" placeholder="Ej: Juan García"></div>
        <div class="fg" style="margin-bottom:0;"><label class="fl">Email <span class="req">*</span></label><input class="fc" id="contactEmail" type="email" placeholder="correo@empresa.com"></div>
      </div>
      <div class="fg"><label class="fl">Asignar a listas <span style="font-weight:400;text-transform:none;letter-spacing:0;">(opcional)</span></label><div class="list-chk-grid" id="listCheckGrid"><div style="color:var(--muted);font-size:12px;padding:8px;">Cargando listas…</div></div></div>
    </div>
    <div class="modal-foot"><button class="btn-mcancel" onclick="closeModal('modalContact')">Cancelar</button><button class="btn-primary" id="btnSaveContact" onclick="saveContact()"><i class="bi bi-check-lg"></i> Guardar destinatario</button></div>
  </div>
</div>

<div class="modal-bg" id="modalConfirm">
  <div class="modal-box" style="max-width:380px;">
    <div class="modal-head" style="color:var(--danger);"><span style="display:flex;align-items:center;gap:8px;"><i class="bi bi-trash-fill"></i> Confirmar eliminación</span><button class="btn-mclose" onclick="closeModal('modalConfirm')"><i class="bi bi-x"></i></button></div>
    <div class="modal-body-p"><p style="font-size:13.5px;" id="confirmMsg">¿Estás seguro?</p></div>
    <div class="modal-foot"><button class="btn-mcancel" onclick="closeModal('modalConfirm')">Cancelar</button><button class="btn-primary" style="background:var(--danger);" id="confirmOkBtn"><i class="bi bi-trash"></i> Eliminar</button></div>
  </div>
</div>

<!-- ══ MODALES ENVÍO (Bootstrap original) ════════════════════ -->
<div class="modal fade" id="confirmModal" tabindex="-1">
  <div class="modal-dialog"><div class="modal-content" style="background:var(--card);border-color:var(--border);color:var(--text);">
    <div class="modal-header" style="background:var(--header);border-color:var(--border);"><h5 class="modal-title" style="display:flex;align-items:center;gap:8px;"><i class="bi bi-send-check-fill" style="color:var(--success);"></i> Confirmar envío</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
    <div class="modal-body" id="confirmBody" style="background:var(--card);"></div>
    <div class="modal-footer" style="background:var(--header);border-color:var(--border);"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button><button type="button" class="btn btn-success" onclick="doSend()"><i class="bi bi-send-fill"></i> Sí, enviar</button></div>
  </div></div>
</div>

<div class="modal fade" id="histModal" tabindex="-1">
  <div class="modal-dialog modal-lg"><div class="modal-content" style="background:var(--card);border-color:var(--border);color:var(--text);">
    <div class="modal-header" style="background:var(--header);border-color:var(--border);"><h5 class="modal-title" style="display:flex;align-items:center;gap:8px;"><i class="bi bi-envelope-open" style="color:var(--primary);"></i> Detalle de envío</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div>
    <div class="modal-body" id="histModalBody" style="background:var(--card);"></div>
    <div class="modal-footer" style="background:var(--header);border-color:var(--border);"><button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cerrar</button><button type="button" class="btn btn-primary" onclick="reuseEmail()"><i class="bi bi-arrow-repeat"></i> Reutilizar</button></div>
  </div></div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<script>
/* ════════════════════════════════════════════════════════════
   NAVEGACIÓN MÓDULOS
════════════════════════════════════════════════════════════ */
let activeModule = 'envio', listasLoaded = false;

function switchMod(mod) {
  activeModule = mod;
  document.querySelectorAll('.mod-tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('.mod-panel').forEach(p => p.classList.remove('active'));
  const cap = s => s.charAt(0).toUpperCase()+s.slice(1);
  document.getElementById('mTab'+cap(mod)).classList.add('active');
  document.getElementById('panel'+cap(mod)).classList.add('active');
  renderGlobalActions(mod);
  if (mod === 'listas' && !listasLoaded) { listasLoaded = true; loadLists(); loadContacts(); }
}

function renderGlobalActions(mod) {
  const el = document.getElementById('pgActions');
  if (mod === 'envio') {
    el.innerHTML = `<button class="btn-preview" onclick="togglePreview()"><i class="bi bi-eye"></i> Vista previa</button>
      <button class="btn-primary green" onclick="sendEmail()"><i class="bi bi-send-fill"></i> Enviar reporte</button>`;
  } else {
    el.innerHTML = `<button class="btn-ghost" onclick="openModalNewList()"><i class="bi bi-plus-circle"></i> Nueva lista</button>
      <button class="btn-primary" onclick="openModalNewContact()"><i class="bi bi-person-plus-fill"></i> Añadir destinatario</button>`;
  }
}

/* ════════════════════════════════════════════════════════════
   SHARED UTILS
════════════════════════════════════════════════════════════ */
const esc = s => (s||'').toString().replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const openModal  = id => document.getElementById(id).classList.add('open');
const closeModal = id => document.getElementById(id).classList.remove('open');
document.querySelectorAll('.modal-bg').forEach(o => o.addEventListener('click', e => { if(e.target===o) o.classList.remove('open'); }));

function toast(msg, type='inf', dur=4500) {
  const icons={ok:'bi-check-circle-fill',err:'bi-x-circle-fill',inf:'bi-info-circle-fill'};
  const clrs ={ok:'var(--success)',err:'var(--danger)',inf:'var(--info)'};
  const el = document.createElement('div');
  el.className = `toast-item ${type}`;
  el.innerHTML = `<i class="bi ${icons[type]}" style="color:${clrs[type]};font-size:17px;flex-shrink:0;"></i><span>${msg}</span>`;
  document.getElementById('toastWrap').appendChild(el);
  setTimeout(()=>{el.style.cssText='opacity:0;transform:translateX(20px);transition:.3s;';setTimeout(()=>el.remove(),300);}, dur);
}
const showToast = toast; // alias para compatibilidad con envío original

/* ════════════════════════════════════════════════════════════
   MÓDULO LISTAS DE DISTRIBUCIÓN — código original intacto
════════════════════════════════════════════════════════════ */
let allLists=[], allContacts=[], currentListId=null, selectedColors='#3b82f6';
const COLORS=['#3b82f6','#10b981','#f59e0b','#ef4444','#8b5cf6','#06b6d4','#f97316','#ec4899','#64748b'];

function hexA(hex,alpha){const r=parseInt((hex||'#3b82f6').slice(1,3),16),g=parseInt((hex||'#3b82f6').slice(3,5),16),b=parseInt((hex||'#3b82f6').slice(5,7),16);return`rgba(${r},${g},${b},${alpha})`;}

async function loadLists(){
  try{
    const j=await fetch('/api/report-lists',{credentials:'include'}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    allLists=j.data||[];
    renderSidebar();
    document.getElementById('mBadgeListas').textContent=allLists.length;
    renderDistrib(); // ← reconstruye chips con listas reales
  }catch(e){toast('Error cargando listas: '+e.message,'err');}
}

function renderSidebar(){
  const el=document.getElementById('listsSidebar');
  const allRow=`<div class="list-item ${currentListId===null?'active':''}" onclick="selectList(null)"><div class="list-dot" style="background:var(--primary);"></div><span class="list-name">Todos los contactos</span><span class="list-count">${allContacts.length}</span></div>`;
  const rows=allLists.map(l=>`<div class="list-item ${currentListId===l.id?'active':''}" onclick="selectList(${l.id})" id="litem_${l.id}"><div class="list-dot" style="background:${esc(l.color||'#3b82f6')};"></div><span class="list-name">${esc(l.name)}</span><span class="list-count">${l.member_count||0}</span><div class="list-actions"><button class="list-edit-btn" onclick="event.stopPropagation();openModalEditList(${l.id})" title="Editar"><i class="bi bi-pencil"></i></button><button class="list-del-btn" onclick="event.stopPropagation();confirmDeleteList(${l.id},'${esc(l.name)}')" title="Eliminar"><i class="bi bi-trash3"></i></button></div></div>`).join('');
  el.innerHTML=allRow+rows;
}

function selectList(id){currentListId=id;renderSidebar();loadContacts();}

async function loadContacts(){
  const tbody=document.getElementById('contactsTbody');
  tbody.innerHTML=`<tr><td colspan="5" class="empty-state"><span class="mini-spin"></span></td></tr>`;
  const title=currentListId===null?'Todos los destinatarios':(allLists.find(l=>l.id===currentListId)?.name||'—');
  document.getElementById('panelTitle').textContent=title;
  try{
    let url='/api/report-lists/contacts/all';
    if(currentListId)url+=`?list_id=${currentListId}`;
    const j=await fetch(url,{credentials:'include'}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    allContacts=j.data||[];
    renderContacts(allContacts);
  }catch(e){tbody.innerHTML=`<tr><td colspan="5" class="empty-state"><i class="bi bi-exclamation-triangle" style="color:var(--danger);"></i><p>${e.message}</p></td></tr>`;}
}

function renderContacts(list){
  const tbody=document.getElementById('contactsTbody');
  document.getElementById('contactCount').textContent=list.length;
  document.getElementById('contactsFooter').textContent=`${list.length} destinatario${list.length!==1?'s':''}`;
  if(!list.length){tbody.innerHTML=`<tr><td colspan="5" class="empty-state"><i class="bi bi-inbox"></i><p>Sin destinatarios${currentListId?' en esta lista':''}</p></td></tr>`;return;}
  tbody.innerHTML=list.map(c=>{
    const listChips=c.lists?c.lists.split(', ').map((ln,i)=>{const lid=(c.list_ids||'').split(',')[i];const color=allLists.find(l=>String(l.id)===String(lid))?.color||'#64748b';return`<span class="list-chip" style="background:${hexA(color,.1)};color:${color};border-color:${hexA(color,.3)};">${esc(ln)}</span>`;}).join(''):'<span style="color:var(--muted);font-size:11px;">Sin lista</span>';
    return`<tr><td style="font-weight:600;">${esc(c.name)}</td><td style="font-size:12px;"><a href="mailto:${esc(c.email)}" style="color:var(--primary);">${esc(c.email)}</a></td><td>${listChips}</td><td><span class="pill ${c.is_active?'p-active':'p-inactive'}">${c.is_active?'✅ Activo':'⛔ Inactivo'}</span></td><td style="white-space:nowrap;"><div style="display:flex;gap:5px;"><button class="btn-sm" onclick="openModalEditContact(${c.id})"><i class="bi bi-pencil"></i></button><button class="btn-sm danger" onclick="confirmDeleteContact(${c.id},'${esc(c.name)}')"><i class="bi bi-trash3"></i></button></div></td></tr>`;
  }).join('');
}

function filterContacts(){const q=document.getElementById('searchContacts').value.toLowerCase();renderContacts(q?allContacts.filter(c=>c.name.toLowerCase().includes(q)||c.email.toLowerCase().includes(q)):allContacts);}

function buildColorPicker(selected){document.getElementById('colorOpts').innerHTML=COLORS.map(c=>`<div class="color-opt${c===selected?' sel':''}" style="background:${c};" onclick="pickColor('${c}')" title="${c}"></div>`).join('');selectedColors=selected||COLORS[0];}
function pickColor(c){selectedColors=c;document.querySelectorAll('.color-opt').forEach(el=>el.classList.toggle('sel',el.style.background===c));}

function openModalNewList(){document.getElementById('listId').value='';document.getElementById('listName').value='';document.getElementById('listDesc').value='';document.getElementById('modalListTitle').innerHTML=`<i class="bi bi-collection-fill" style="color:var(--primary);"></i> Nueva lista`;buildColorPicker(COLORS[0]);openModal('modalList');setTimeout(()=>document.getElementById('listName').focus(),150);}
async function openModalEditList(id){const l=allLists.find(x=>x.id===id);if(!l)return;document.getElementById('listId').value=l.id;document.getElementById('listName').value=l.name;document.getElementById('listDesc').value=l.description||'';document.getElementById('modalListTitle').innerHTML=`<i class="bi bi-pencil-square" style="color:var(--primary);"></i> Editar lista`;buildColorPicker(l.color||COLORS[0]);openModal('modalList');}
async function saveList(){const id=document.getElementById('listId').value;const name=document.getElementById('listName').value.trim();const desc=document.getElementById('listDesc').value.trim();if(!name){document.getElementById('listName').classList.add('invalid');return;}document.getElementById('listName').classList.remove('invalid');document.getElementById('btnSaveList').disabled=true;try{const method=id?'PUT':'POST';const url=id?`/api/report-lists/${id}`:'/api/report-lists';const j=await fetch(url,{method,credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,description:desc||null,color:selectedColors,icon:'bi-people'})}).then(r=>r.json());if(!j.success)throw new Error(j.error);toast(id?'Lista actualizada':'Lista creada','ok');closeModal('modalList');await loadLists();await loadContacts();}catch(e){toast('Error: '+e.message,'err');}finally{document.getElementById('btnSaveList').disabled=false;}}

function buildListCheckGrid(selectedIds=[]){const grid=document.getElementById('listCheckGrid');if(!allLists.length){grid.innerHTML=`<div style="color:var(--muted);font-size:12px;padding:8px;">Sin listas disponibles — crea una primero</div>`;return;}grid.innerHTML=allLists.map(l=>{const checked=selectedIds.includes(l.id)||selectedIds.includes(String(l.id));return`<label class="list-chk-row${checked?' checked':''}" id="chk_row_${l.id}"><input type="checkbox" value="${l.id}" ${checked?'checked':''} onchange="toggleChkRow(this,${l.id})"><div class="list-chk-dot" style="background:${esc(l.color||'#3b82f6')};"></div><span class="list-chk-name">${esc(l.name)}</span><span style="font-size:11px;color:var(--muted);">${l.member_count||0} miembros</span><div class="list-chk-icon"><i class="bi bi-check"></i></div></label>`;}).join('');}
const toggleChkRow=(inp,id)=>document.getElementById('chk_row_'+id).classList.toggle('checked',inp.checked);
const getCheckedLists=()=>[...document.querySelectorAll('#listCheckGrid input:checked')].map(i=>+i.value);

function openModalNewContact(){document.getElementById('contactId').value='';document.getElementById('contactName').value='';document.getElementById('contactEmail').value='';document.getElementById('contactName').classList.remove('invalid');document.getElementById('contactEmail').classList.remove('invalid');document.getElementById('modalContactTitle').innerHTML=`<i class="bi bi-person-plus-fill" style="color:var(--success);"></i> Añadir destinatario`;buildListCheckGrid(currentListId?[currentListId]:[]);openModal('modalContact');setTimeout(()=>document.getElementById('contactName').focus(),150);}
async function openModalEditContact(id){const c=allContacts.find(x=>x.id===id);if(!c)return;document.getElementById('contactId').value=c.id;document.getElementById('contactName').value=c.name;document.getElementById('contactEmail').value=c.email;document.getElementById('contactName').classList.remove('invalid');document.getElementById('contactEmail').classList.remove('invalid');document.getElementById('modalContactTitle').innerHTML=`<i class="bi bi-pencil-square" style="color:var(--primary);"></i> Editar destinatario`;buildListCheckGrid((c.list_ids||'').split(',').filter(Boolean));openModal('modalContact');}
async function saveContact(){const id=document.getElementById('contactId').value;const name=document.getElementById('contactName').value.trim();const email=document.getElementById('contactEmail').value.trim();let valid=true;if(!name){document.getElementById('contactName').classList.add('invalid');valid=false;}else document.getElementById('contactName').classList.remove('invalid');if(!email||!/^[^\s@]+@[^\s@]+\.[^\s@]+$/.test(email)){document.getElementById('contactEmail').classList.add('invalid');valid=false;}else document.getElementById('contactEmail').classList.remove('invalid');if(!valid)return;const list_ids=getCheckedLists();document.getElementById('btnSaveContact').disabled=true;try{const method=id?'PUT':'POST';const url=id?`/api/report-lists/contacts/${id}`:'/api/report-lists/contacts';const j=await fetch(url,{method,credentials:'include',headers:{'Content-Type':'application/json'},body:JSON.stringify({name,email,list_ids})}).then(r=>r.json());if(!j.success)throw new Error(j.error);toast(id?'Destinatario actualizado':`✅ ${name} añadido correctamente`,'ok');closeModal('modalContact');await loadLists();await loadContacts();}catch(e){toast('Error: '+e.message,'err');}finally{document.getElementById('btnSaveContact').disabled=false;}}

function confirmDeleteList(id,name){document.getElementById('confirmMsg').innerHTML=`¿Eliminar la lista <strong>"${esc(name)}"</strong>? Los contactos no se borrarán, solo se desvinculan de la lista.`;document.getElementById('confirmOkBtn').onclick=async()=>{try{const j=await fetch(`/api/report-lists/${id}`,{method:'DELETE',credentials:'include'}).then(r=>r.json());if(!j.success)throw new Error(j.error);toast('Lista eliminada','ok');closeModal('modalConfirm');if(currentListId===id)currentListId=null;await loadLists();await loadContacts();}catch(e){toast('Error: '+e.message,'err');}};openModal('modalConfirm');}
function confirmDeleteContact(id,name){document.getElementById('confirmMsg').innerHTML=`¿Eliminar a <strong>"${esc(name)}"</strong> de todos los destinatarios? Esta acción no se puede deshacer.`;document.getElementById('confirmOkBtn').onclick=async()=>{try{const j=await fetch(`/api/report-lists/contacts/${id}`,{method:'DELETE',credentials:'include'}).then(r=>r.json());if(!j.success)throw new Error(j.error);toast('Destinatario eliminado','ok');closeModal('modalConfirm');await loadLists();await loadContacts();}catch(e){toast('Error: '+e.message,'err');}};openModal('modalConfirm');}

function exportContacts(){if(!allContacts.length){toast('Sin datos para exportar','inf');return;}const rows=[['Nombre','Email','Listas','Estado'],...allContacts.map(c=>[c.name,c.email,c.lists||'',c.is_active?'Activo':'Inactivo'])].map(r=>r.map(v=>`"${(v||'').toString().replace(/"/g,'""')}"`).join(','));const a=Object.assign(document.createElement('a'),{href:URL.createObjectURL(new Blob([rows.join('\n')],{type:'text/csv;charset=utf-8;'})),download:`destinatarios_${new Date().toISOString().split('T')[0]}.csv`});a.click();toast(`Exportados: ${allContacts.length} contactos`,'ok');}

/* ════════════════════════════════════════════════════════════
   MÓDULO ENVÍO DE REPORTES — código original intacto
   INTEGRACIÓN: renderDistrib() ahora usa listas reales de BD
════════════════════════════════════════════════════════════ */
const REPORT_DEFS=[
  {id:'empleados',    name:'Reporte de Empleados',        icon:'bi-people-fill',         color:'#3b82f6',type:'PDF',  size:'~280 KB'},
  {id:'asignaciones', name:'Asignaciones Activas',        icon:'bi-clipboard-check-fill', color:'#10b981',type:'Excel',size:'~195 KB'},
  {id:'print-queue',  name:'Cola de Impresión',           icon:'bi-printer-fill',         color:'#f97316',type:'PDF',  size:'~110 KB'},
  {id:'kpis',         name:'KPIs del Mes',                icon:'bi-bar-chart-fill',       color:'#8b5cf6',type:'PDF',  size:'~450 KB'},
  {id:'inventario',   name:'Inventario de Equipos',       icon:'bi-laptop-fill',          color:'#10b981',type:'Excel',size:'~320 KB'},
  {id:'mantenimiento',name:'Historial de Mantenimientos', icon:'bi-tools',                color:'#f59e0b',type:'PDF',  size:'~175 KB'},
];

// Listas estáticas de respaldo (igual que el original)
const STATIC_DISTRIB=[
  {id:'gerencia',   label:'Gerencia',   icon:'bi-building',  emails:['gerencia@empresa.com']},
  {id:'rrhh',       label:'RRHH',       icon:'bi-people',    emails:['rrhh@empresa.com']},
  {id:'ti',         label:'TI',         icon:'bi-cpu',       emails:['ti@empresa.com']},
  {id:'finanzas',   label:'Finanzas',   icon:'bi-cash-stack',emails:['finanzas@empresa.com']},
  {id:'operaciones',label:'Operaciones',icon:'bi-gear',      emails:['operaciones@empresa.com']},
];

const TEMPLATES=[
  {label:'📊 KPIs del mes',  fn:()=>`[Reporte] KPIs – ${mesActual()} ${anio()}`},
  {label:'👥 Personal',      fn:()=>`[RRHH] Reporte de Personal – ${hoy()}`},
  {label:'💻 Inventario TI', fn:()=>`[TI] Inventario de Equipos – ${hoy()}`},
  {label:'🖨️ Cola impresión',fn:()=>`[Impresión] Resumen cola – ${hoy()}`},
  {label:'📋 Asignaciones',  fn:()=>`[Asignaciones] Estado actual – ${hoy()}`},
];

let tags={to:[],cc:[],bcc:[]},attachments=[],selectedReps=new Set(),repBlobs={};
let histItems=loadHist(),currentHistItem=null,acTimers={},employees=[];

const pad=n=>String(n).padStart(2,'0');
const hoy=()=>{const d=new Date();return`${pad(d.getDate())}/${pad(d.getMonth()+1)}/${d.getFullYear()}`;};
const MESES=['Enero','Febrero','Marzo','Abril','Mayo','Junio','Julio','Agosto','Septiembre','Octubre','Noviembre','Diciembre'];
const mesActual=()=>MESES[new Date().getMonth()];
const anio=()=>new Date().getFullYear();
const fmtSize=b=>{if(!b||typeof b==='string')return b||'—';if(b<1024)return b+'B';if(b<1048576)return(b/1024).toFixed(1)+'KB';return(b/1048576).toFixed(1)+'MB';};
const isToday=d=>{const n=new Date();return d.getDate()===n.getDate()&&d.getMonth()===n.getMonth()&&d.getFullYear()===n.getFullYear();};

function setDefaultBody(){
  const b=document.getElementById('body');
  if(b&&!b.value)b.value=`Estimado equipo,\n\nAdjunto el reporte solicitado generado el ${hoy()}.\n\nSaludos,\nSistema EquipManager`;
  updatePreview();
}

async function loadEmployees(){
  try{const j=await fetch('/api/employees?limit=500&page=1',{credentials:'include'}).then(r=>r.json());if(j.success&&j.data)employees=j.data;}
  catch(e){console.warn('Autocomplete empleados no disponible:',e.message);}
}

/* renderDistrib: usa listas reales de la BD si están cargadas,
   si no usa las estáticas de respaldo (igual que el original) */
function renderDistrib(){
  const el=document.getElementById('dcRow');
  if(!el)return;
  // Preferir listas reales de la BD
  const source = allLists.length ? allLists : STATIC_DISTRIB;
  el.innerHTML=source.map(l=>{
    const isReal=!!l.member_count!==undefined&&allLists.length>0;
    const id=isReal?`real_${l.id}`:l.id;
    const icon=l.icon||'bi-collection';
    const label=l.name||l.label;
    const count=isReal?(l.member_count||0):((l.emails||[]).length);
    return`<button class="dc" id="dc_${id}" onclick="toggleDistrib('${id}','${esc(label)}',${isReal?l.id:'null'})">
      <i class="bi ${icon}"></i> ${esc(label)}
      <span style="font-size:10px;opacity:.7;">(${count})</span>
    </button>`;
  }).join('');
}

/* toggleDistrib para listas reales: añade TODOS los emails de esa lista
   cargándolos del API (si es lista real) o usando el array estático */
async function toggleDistrib(chipId, label, realListId){
  const btn=document.getElementById('dc_'+chipId);
  if(!btn)return;

  if(realListId){
    // Lista real: cargar emails desde la BD
    try{
      const url=`/api/report-lists/contacts/all?list_id=${realListId}`;
      const j=await fetch(url,{credentials:'include'}).then(r=>r.json());
      if(!j.success)throw new Error(j.error);
      const emails=(j.data||[]).filter(c=>c.is_active&&c.email).map(c=>c.email);
      if(!emails.length){toast(`La lista "${label}" no tiene contactos activos`,'inf');return;}
      const allIn=emails.every(e=>tags.to.some(t=>t.email===e));
      if(allIn){emails.forEach(e=>{const i=tags.to.findIndex(t=>t.email===e);if(i>-1)tags.to.splice(i,1);});btn.classList.remove('active');}
      else{emails.forEach(e=>{if(!tags.to.some(t=>t.email===e))tags.to.push({email:e,name:e,internal:false,distrib:true});});btn.classList.add('active');}
    }catch(e){toast('Error cargando lista: '+e.message,'err');return;}
  }else{
    // Lista estática
    const list=STATIC_DISTRIB.find(d=>d.id===chipId);
    if(!list)return;
    const allIn=list.emails.every(e=>tags.to.some(t=>t.email===e));
    if(allIn){list.emails.forEach(e=>{const i=tags.to.findIndex(t=>t.email===e);if(i>-1)tags.to.splice(i,1);});btn.classList.remove('active');}
    else{list.emails.forEach(e=>{if(!tags.to.some(t=>t.email===e))tags.to.push({email:e,name:e,internal:false,distrib:true});});btn.classList.add('active');}
  }
  renderTags('to');updatePreview();updateRcCount();
}

/* ── TAGS (original intacto) ── */
const focusTI=id=>document.getElementById(id)?.focus();
function tagKD(e,field){const inp=e.target;if((e.key==='Enter'||e.key===',')&&inp.value.trim()){e.preventDefault();addTag(field,inp.value.trim());inp.value='';closeAc(field+'Ac');}else if(e.key==='Backspace'&&!inp.value&&tags[field].length)removeTag(field,tags[field].length-1);}
function addTag(field,email,name){const clean=email.replace(/,/g,'').trim();if(!clean||tags[field].some(t=>t.email===clean))return;const internal=employees.some(e=>e.email===clean);const distrib=STATIC_DISTRIB.some(d=>d.emails.includes(clean));tags[field].push({email:clean,name:name||clean,internal,distrib});renderTags(field);updatePreview();updateRcCount();}
function removeTag(field,idx){tags[field].splice(idx,1);renderTags(field);updatePreview();updateRcCount();}
function renderTags(field){const wrap=document.getElementById(field+'Wrap');const inp=document.getElementById(field+'Input');wrap.querySelectorAll('.tag').forEach(t=>t.remove());tags[field].forEach((t,i)=>{const cls=t.distrib?'dist':t.internal?'':'ext';const d=document.createElement('div');d.className=`tag ${cls}`;const short=t.name!==t.email?t.name:(t.email.length>22?t.email.split('@')[0]+'@…':t.email);d.innerHTML=`<span title="${esc(t.email)}">${esc(short)}</span><button class="tag-x" onclick="removeTag('${field}',${i});event.stopPropagation()">×</button>`;wrap.insertBefore(d,inp);});}
const updateRcCount=()=>{const el=document.getElementById('rcCount');if(el)el.textContent=tags.to.length+tags.cc.length+tags.bcc.length;};

/* ── AUTOCOMPLETE (original intacto) ── */
function acSearch(inp,dropId,field){const q=inp.value.toLowerCase().trim();clearTimeout(acTimers[field]);if(q.length<2){closeAc(dropId);return;}acTimers[field]=setTimeout(()=>{const matches=employees.filter(e=>(e.full_name||'').toLowerCase().includes(q)||(e.email||'').toLowerCase().includes(q)).slice(0,8);const drop=document.getElementById(dropId);if(!matches.length){closeAc(dropId);return;}drop.innerHTML=matches.map(e=>{const init=(e.full_name||e.email||'?').split(' ').map(w=>w[0]).slice(0,2).join('').toUpperCase();return`<div class="ac-item" onclick="pickAc('${field}','${esc(e.email)}','${esc(e.full_name||e.email)}')"><div class="ac-av">${init}</div><div><div class="ac-name">${esc(e.full_name||e.email)}</div><div class="ac-email">${esc(e.email)}</div></div><span class="ac-dept">${esc(e.department||e.position_name||'')}</span></div>`;}).join('');drop.style.display='block';},200);}
const pickAc=(field,email,name)=>{addTag(field,email,name);document.getElementById(field+'Input').value='';closeAc(field+'Ac');};
const closeAc=id=>{const d=document.getElementById(id);if(d)d.style.display='none';};
document.addEventListener('click',e=>{['toAc','ccAc','bccAc'].forEach(id=>{if(!document.getElementById(id)?.contains(e.target)&&!e.target.closest('.tags-wrap'))closeAc(id);});});

function toggleCC(field){const row=document.getElementById(field+'Row');const btn=document.getElementById(field+'Toggle');const vis=row.style.display!=='none';row.style.display=vis?'none':'';btn.classList.toggle('on',!vis);btn.textContent=vis?`+ ${field.toUpperCase()}`:`− ${field.toUpperCase()}`;updatePreview();}

function renderTemplates(){const el=document.getElementById('tplRow');if(!el)return;el.innerHTML=TEMPLATES.map(t=>`<button class="tpl-btn" onclick="applyTpl('${esc(t.fn())}')">${t.label}</button>`).join('');}
const applyTpl=text=>{document.getElementById('subject').value=text;updatePreview();};

function switchFT(tab){document.getElementById('panelUpload').style.display=tab==='upload'?'':'none';document.getElementById('panelApp').style.display=tab==='app'?'':'none';document.getElementById('ftUpload').classList.toggle('active',tab==='upload');document.getElementById('ftApp').classList.toggle('active',tab==='app');if(tab==='app')renderAppReports();}

function initDrop(){const zone=document.getElementById('dropZone');if(!zone)return;zone.addEventListener('dragover',e=>{e.preventDefault();zone.classList.add('drag-over');});zone.addEventListener('dragleave',()=>zone.classList.remove('drag-over'));zone.addEventListener('drop',e=>{e.preventDefault();zone.classList.remove('drag-over');handleFiles(e.dataTransfer.files);});}
function handleFiles(files){Array.from(files).forEach(f=>{if(attachments.some(a=>a.name===f.name&&a.type==='upload'))return;if(f.size>25*1024*1024){showToast(`${f.name} excede 25 MB`,'err');return;}attachments.push({id:`up_${Date.now()}_${Math.random().toString(36).slice(2)}`,name:f.name,size:f.size,file:f,type:'upload'});});renderAtts();updatePreview();}

function renderAppReports(){const wrap=document.getElementById('repListWrap');if(!wrap)return;wrap.innerHTML=`<div class="rep-list">`+REPORT_DEFS.map(r=>`<div class="rep-item" id="repI_${r.id}" onclick="toggleRep('${r.id}')"><div class="rep-check" id="repCk_${r.id}"><i class="bi bi-check"></i></div><i class="bi ${r.icon}" style="font-size:20px;color:${r.color};flex-shrink:0;"></i><div><div class="rep-name">${r.name}</div><div class="rep-meta">${r.size} · Generado al vuelo</div></div><span class="rep-badge ${r.type==='PDF'?'rb-pdf':'rb-xls'}">${r.type}</span><div class="rep-gen-overlay"><span class="mini-spin" style="border-color:rgba(59,130,246,.3);border-top-color:var(--primary);"></span> Generando…</div></div>`).join('')+`</div>`;}

async function toggleRep(id){const def=REPORT_DEFS.find(r=>r.id===id);const item=document.getElementById('repI_'+id);if(!def||!item)return;if(selectedReps.has(id)){selectedReps.delete(id);item.classList.remove('sel');attachments=attachments.filter(a=>a.id!==id);delete repBlobs[id];renderAtts();updatePreview();return;}item.classList.add('generating');showToast(`Generando ${def.name}…`,'inf',6000);try{const resp=await fetch(`/api/reports/${def.id}`,{method:'GET',credentials:'include'});if(!resp.ok)throw new Error(`HTTP ${resp.status}`);const cd=resp.headers.get('Content-Disposition')||'';const fnM=cd.match(/filename[^;=\n]*=['"]?([^'";\n]+)/i);const filename=fnM?fnM[1].trim():`${def.id}.${def.type==='PDF'?'pdf':'xlsx'}`;const blob=await resp.blob();repBlobs[id]={blob,filename};selectedReps.add(id);item.classList.add('sel');attachments.push({id,name:filename,size:blob.size,type:'app',def});showToast(`✅ ${def.name} listo`,'ok',3000);renderAtts();updatePreview();}catch(e){showToast(`❌ Error generando ${def.name}: ${e.message}`,'err',6000);}finally{item.classList.remove('generating');}}

function fileIcon(name){const ext=('.'+(name.split('.').pop()||'')).toLowerCase();if(['.pdf'].includes(ext))return{cls:'att-pdf',icon:'bi-file-earmark-pdf'};if(['.doc','.docx'].includes(ext))return{cls:'att-word',icon:'bi-file-earmark-word'};if(['.xls','.xlsx','.csv'].includes(ext))return{cls:'att-excel',icon:'bi-file-earmark-excel'};if(['.ppt','.pptx'].includes(ext))return{cls:'att-ppt',icon:'bi-file-earmark-ppt'};if(['.png','.jpg','.jpeg'].includes(ext))return{cls:'att-img',icon:'bi-file-image'};return{cls:'att-other',icon:'bi-file-earmark'};}

function renderAtts(){const list=document.getElementById('attList');const cnt=document.getElementById('attCount');if(cnt)cnt.textContent=attachments.length;if(!attachments.length){list.innerHTML='';return;}list.innerHTML=attachments.map((a,i)=>{const fi=fileIcon(a.name);const src=a.type==='app'?`<span class="att-src app">APP</span>`:`<span class="att-src upload">SUBIDO</span>`;return`<div class="att-item"><i class="bi ${fi.icon} att-icon ${fi.cls}"></i><div class="att-info"><div class="att-name">${esc(a.name)}</div><div class="att-size">${fmtSize(a.size)} ${src}</div></div><button class="att-rm" onclick="removeAtt(${i})" title="Quitar adjunto"><i class="bi bi-x-lg" style="font-size:12px;"></i></button></div>`;}).join('');}
function removeAtt(idx){const a=attachments[idx];if(a.type==='app'){selectedReps.delete(a.id);document.getElementById('repI_'+a.id)?.classList.remove('sel');delete repBlobs[a.id];}attachments.splice(idx,1);renderAtts();updatePreview();}

function togglePreview(){const panel=document.getElementById('previewPanel');const btn=document.getElementById('btnPreview');const vis=panel.style.display!=='none';panel.style.display=vis?'none':'';if(btn)btn.classList.toggle('on',!vis);if(!vis)updatePreview();}
function updatePreview(){const toStr=tags.to.map(t=>t.name!==t.email?`${t.name} <${t.email}>`:t.email).join(', ')||'—';const ccStr=tags.cc.map(t=>t.email).join(', ');const bccStr=tags.bcc.map(t=>t.email).join(', ');const s=id=>{const el=document.getElementById(id);return el};if(s('pvTo'))document.getElementById('pvTo').textContent=toStr;if(s('pvSubject'))document.getElementById('pvSubject').textContent=document.getElementById('subject')?.value||'(sin asunto)';if(s('pvBody'))document.getElementById('pvBody').textContent=document.getElementById('body')?.value||'';const ccRow=document.getElementById('pvCcRow'),bccRow=document.getElementById('pvBccRow');if(ccRow){if(ccStr){ccRow.style.display='';document.getElementById('pvCc').textContent=ccStr;}else ccRow.style.display='none';}if(bccRow){if(bccStr){bccRow.style.display='';document.getElementById('pvBcc').textContent=bccStr;}else bccRow.style.display='none';}const pvAtts=document.getElementById('pvAtts');if(pvAtts)pvAtts.innerHTML=attachments.map(a=>{const fi=fileIcon(a.name);return`<div class="ep-att-chip"><i class="bi ${fi.icon} ${fi.cls}"></i> ${esc(a.name)}</div>`;}).join('');}

function shakeEl(id){const el=document.getElementById(id);if(!el)return;el.style.animation='shk .4s ease';el.style.borderColor='var(--danger)';el.style.boxShadow='0 0 0 3px rgba(239,68,68,.12)';setTimeout(()=>{el.style.animation='';el.style.borderColor='';el.style.boxShadow='';},500);}
const shkStyle=document.createElement('style');shkStyle.textContent='@keyframes shk{0%,100%{transform:translateX(0);}20%,60%{transform:translateX(-5px);}40%,80%{transform:translateX(5px);}}';document.head.appendChild(shkStyle);

function sendEmail(){if(!tags.to.length){shakeEl('toWrap');showToast('Añade al menos un destinatario','inf');return;}if(!document.getElementById('subject').value.trim()){shakeEl('subject');showToast('Escribe el asunto','inf');return;}const toStr=tags.to.map(t=>t.name!==t.email?`${t.name} <${t.email}>`:t.email).join('\n');const ccStr=tags.cc.map(t=>t.email).join(', ');const bccStr=tags.bcc.map(t=>t.email).join(', ');let html=`<div class="confirm-row"><div class="confirm-key">Para</div><div class="confirm-val">${esc(toStr)}</div></div>${ccStr?`<div class="confirm-row"><div class="confirm-key">CC</div><div class="confirm-val">${esc(ccStr)}</div></div>`:''}${bccStr?`<div class="confirm-row"><div class="confirm-key">CCO</div><div class="confirm-val">${esc(bccStr)}</div></div>`:''}<div class="confirm-row"><div class="confirm-key">Asunto</div><div class="confirm-val" style="font-weight:700;">${esc(document.getElementById('subject').value)}</div></div>`;if(attachments.length)html+=`<div class="confirm-row"><div class="confirm-key">Adjuntos (${attachments.length})</div><div class="confirm-val">${attachments.map(a=>`📎 ${esc(a.name)} (${fmtSize(a.size)})`).join('\n')}</div></div>`;document.getElementById('confirmBody').innerHTML=html;new bootstrap.Modal(document.getElementById('confirmModal')).show();}

async function doSend(){bootstrap.Modal.getInstance(document.getElementById('confirmModal'))?.hide();const btnSend=document.getElementById('btnSend');btnSend.disabled=true;showSrStatus('Enviando correo…','sending');document.getElementById('miniProg').style.display='';try{const fd=new FormData();fd.append('to',JSON.stringify(tags.to.map(t=>t.email)));fd.append('cc',JSON.stringify(tags.cc.map(t=>t.email)));fd.append('bcc',JSON.stringify(tags.bcc.map(t=>t.email)));fd.append('subject',document.getElementById('subject').value);fd.append('body',document.getElementById('body').value);attachments.filter(a=>a.type==='upload').forEach(a=>fd.append('files',a.file,a.name));Object.entries(repBlobs).forEach(([,{blob,filename}])=>fd.append('files',blob,filename));const resp=await fetch('/api/mailer/send',{method:'POST',body:fd,credentials:'include'});const result=await resp.json();if(!result.success)throw new Error(result.error||'Error desconocido');saveHistEntry('ok');showSrStatus(`✅ Correo enviado correctamente a ${tags.to.length} destinatario(s)`,'ok');showToast('✅ Correo enviado','ok');setTimeout(()=>{clearForm(true);document.getElementById('srStatus').classList.remove('show');},3500);}catch(e){saveHistEntry('err');showSrStatus(`❌ Error al enviar: ${e.message}`,'err');showToast('Error: '+e.message,'err',7000);}finally{document.getElementById('miniProg').style.display='none';btnSend.disabled=false;}}

function saveHistEntry(status){histItems.unshift({id:Date.now(),to:tags.to.map(t=>t.email),cc:tags.cc.map(t=>t.email),bcc:tags.bcc.map(t=>t.email),subject:document.getElementById('subject').value,body:document.getElementById('body').value,atts:attachments.map(a=>({name:a.name,size:fmtSize(a.size),type:a.type})),sentAt:new Date().toISOString(),status});saveHist();renderHist();document.getElementById('mBadgeEnvio').textContent=histItems.length;}

function showSrStatus(msg,type){const el=document.getElementById('srStatus');el.className=`sr-status show ${type}`;el.innerHTML=type==='sending'?`<span class="mini-spin" style="border-color:rgba(6,182,212,.3);border-top-color:var(--info);"></span><span>${msg}</span>`:`<span>${msg}</span>`;if(type!=='sending')setTimeout(()=>el.classList.remove('show'),6000);}

function loadHist(){try{return JSON.parse(localStorage.getItem('srMailHist')||'[]');}catch{return[];}}
function saveHist(){try{localStorage.setItem('srMailHist',JSON.stringify(histItems.slice(0,60)));}catch{}}
function renderHist(q=''){const list=document.getElementById('histList');if(!list)return;const items=q?histItems.filter(h=>h.subject.toLowerCase().includes(q)||h.to.join(',').toLowerCase().includes(q)):histItems;if(!items.length){list.innerHTML=`<div class="hist-empty"><i class="bi bi-inbox"></i>Sin envíos recientes</div>`;return;}list.innerHTML=items.map(h=>{const d=new Date(h.sentAt);const ts=isToday(d)?d.toLocaleTimeString('es-PE',{hour:'2-digit',minute:'2-digit'}):d.toLocaleDateString('es-PE',{day:'2-digit',month:'short'});return`<div class="hi" onclick="openHistDetail(${h.id})"><div class="hi-top"><div class="hi-subj">${esc(h.subject)}</div><div class="hi-time">${ts}</div></div><div class="hi-to">Para: ${h.to.slice(0,2).join(', ')}${h.to.length>2?` +${h.to.length-2}`:''}</div><div class="hi-badges"><span class="hb ${h.status==='ok'?'hb-ok':'hb-err'}">${h.status==='ok'?'✅ Enviado':'❌ Error'}</span>${h.atts.length?`<span class="hb hb-att">📎 ${h.atts.length}</span>`:''}</div></div>`;}).join('');}
const filterHist=()=>renderHist(document.getElementById('histSearch').value.toLowerCase());
function openHistDetail(id){const h=histItems.find(x=>x.id===id);if(!h)return;currentHistItem=h;const d=new Date(h.sentAt);document.getElementById('histModalBody').innerHTML=`<div style="display:flex;flex-direction:column;gap:9px;"><div class="confirm-row"><div class="confirm-key">Para</div><div class="confirm-val">${esc(h.to.join(', '))}</div></div>${h.cc.length?`<div class="confirm-row"><div class="confirm-key">CC</div><div class="confirm-val">${esc(h.cc.join(', '))}</div></div>`:''}${h.bcc.length?`<div class="confirm-row"><div class="confirm-key">CCO</div><div class="confirm-val">${esc(h.bcc.join(', '))}</div></div>`:''}<div class="confirm-row"><div class="confirm-key">Asunto</div><div class="confirm-val" style="font-weight:700;">${esc(h.subject)}</div></div><div class="confirm-row"><div class="confirm-key">Enviado</div><div class="confirm-val" style="font-family:'DM Mono',monospace;font-size:12px;">${d.toLocaleString('es-PE')}</div></div>${h.body?`<div class="confirm-row"><div class="confirm-key">Mensaje</div><div class="confirm-val" style="font-weight:400;white-space:pre-wrap;">${esc(h.body)}</div></div>`:''}${h.atts.length?`<div class="confirm-row"><div class="confirm-key">Adjuntos</div><div class="confirm-val">${h.atts.map(a=>`📎 ${esc(a.name)} (${a.size})`).join('\n')}</div></div>`:''}</div>`;new bootstrap.Modal(document.getElementById('histModal')).show();}
function reuseEmail(){if(!currentHistItem)return;bootstrap.Modal.getInstance(document.getElementById('histModal'))?.hide();const h=currentHistItem;tags.to=h.to.map(e=>({email:e,name:e,internal:false}));tags.cc=h.cc.map(e=>({email:e,name:e,internal:false}));tags.bcc=h.bcc.map(e=>({email:e,name:e,internal:false}));['to','cc','bcc'].forEach(f=>renderTags(f));if(h.cc.length){document.getElementById('ccRow').style.display='';document.getElementById('ccToggle').textContent='− CC';document.getElementById('ccToggle').classList.add('on');}if(h.bcc.length){document.getElementById('bccRow').style.display='';document.getElementById('bccToggle').textContent='− CCO';document.getElementById('bccToggle').classList.add('on');}document.getElementById('subject').value=h.subject;document.getElementById('body').value=h.body||'';updatePreview();updateRcCount();showToast('Formulario rellenado desde historial','ok',3000);}

function clearForm(keepBody=false){tags={to:[],cc:[],bcc:[]};['to','cc','bcc'].forEach(f=>renderTags(f));attachments=[];selectedReps.clear();repBlobs={};renderAtts();renderAppReports();document.getElementById('subject').value='';if(!keepBody)setDefaultBody();document.getElementById('ccRow').style.display='none';document.getElementById('bccRow').style.display='none';document.getElementById('ccToggle').textContent='+ CC';document.getElementById('bccToggle').textContent='+ CCO';document.getElementById('ccToggle').classList.remove('on');document.getElementById('bccToggle').classList.remove('on');document.querySelectorAll('.dc').forEach(c=>c.classList.remove('active'));updatePreview();updateRcCount();}

/* ════════════════════════════════════════════════════════════
   INIT
════════════════════════════════════════════════════════════ */
document.addEventListener('DOMContentLoaded', async () => {
  renderGlobalActions('envio');
  // Cargar listas (necesario para chips de distribución en envío)
  await loadLists();
  // Inicializar módulo envío
  await loadEmployees();
  renderTemplates();
  renderAppReports();
  setDefaultBody();
  updateRcCount();
  renderHist();
  initDrop();
  document.getElementById('mBadgeEnvio').textContent = histItems.length;
});
</script>

<!-- Inyectar variables de entorno al frontend -->
<script>
  window.APP_URL = '<%= APP_URL %>';
</script>

<script src="/js/equipment-dashboard.js"></script>
</body>
</html>
