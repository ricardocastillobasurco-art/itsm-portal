<!DOCTYPE html>
<html lang="es">
<head>
<script>(function(){var p=new URLSearchParams(window.location.search);document.documentElement.setAttribute('data-theme',p.get('theme')||localStorage.getItem('dashboardTheme')||'light');})();</script>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width, initial-scale=1.0">
<title>Cola de Impresión</title>
<link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
<link rel="stylesheet" href="../css/core/style.css">
<link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.1/font/bootstrap-icons.css" rel="stylesheet">
<link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
<script src="https://cdn.jsdelivr.net/npm/chart.js@4.4.0/dist/chart.umd.min.js"></script>
<style>
@import url('https://fonts.googleapis.com/css2?family=Barlow:ital,wght@0,400;0,600;0,700;0,800;0,900;1,700&family=Barlow+Condensed:wght@600;700;800;900&family=JetBrains+Mono:wght@400;600&display=swap');

:root,[data-theme="light"]{
  --bg:#eef2f7;--card:#ffffff;--header:#f4f7fb;
  --border:#dce4ef;--text:#0d1b2e;--muted:#5a7194;
  --primary:#0a3d8f;--primary-light:#1254c4;--primary-glow:rgba(10,61,143,.15);
  --success:#0a8a5c;--warning:#c97700;--danger:#c0392b;
  --info:#0077b6;--gold:#c9860a;--gold-bg:rgba(201,134,10,.07);
  --Workplace:#cc0000;--Workplace-dark:#990000;
  --row-hover:#f0f5ff;--shadow:0 1px 4px rgba(10,30,70,.08);
  --shadow-md:0 6px 24px rgba(10,30,70,.13);--shadow-lg:0 16px 48px rgba(10,30,70,.18);
  --input-bg:#f4f7fb;--radius:12px;
  --font:'Barlow',system-ui,sans-serif;--font-mono:'JetBrains Mono',monospace;
  --font-display:'Barlow Condensed',sans-serif;
}
[data-theme="dark"]{
  --bg:#070f1e;--card:#0d1a2e;--header:#0a1525;--border:#1a2d4a;--text:#e2eaf5;--muted:#6b8ab0;
  --primary:#1a6aff;--primary-light:#4d8fff;--primary-glow:rgba(26,106,255,.18);
  --success:#0fbf7f;--warning:#f0a500;--danger:#e05252;
  --info:#00adef;--gold:#f0a500;--gold-bg:rgba(240,165,0,.08);
  --Workplace:#ff2222;--Workplace-dark:#cc0000;
  --row-hover:#0f2040;--shadow:0 1px 4px rgba(0,0,0,.4);
  --shadow-md:0 6px 24px rgba(0,0,0,.5);--shadow-lg:0 16px 48px rgba(0,0,0,.6);
  --input-bg:#0a1525;
}

*{box-sizing:border-box;margin:0;padding:0;}
body{background:var(--bg);color:var(--text);font-family:var(--font);transition:background .3s,color .3s;font-size:13.5px;line-height:1.5;}
.pg{margin-top:0;padding:0;display:flex;flex-direction:column;gap:0;}

/* ── HERO BANNER ── */
.hero-banner{position:relative;overflow:hidden;background:linear-gradient(135deg,#051530 0%,#0a2560 45%,#0d3380 70%,#0a1e4a 100%);padding:0 32px;min-height:110px;display:flex;align-items:center;border-bottom:3px solid var(--Workplace);}
.hero-banner::before{content:'';position:absolute;inset:0;background:repeating-linear-gradient(90deg,transparent,transparent 60px,rgba(255,255,255,.018) 60px,rgba(255,255,255,.018) 61px),repeating-linear-gradient(0deg,transparent,transparent 40px,rgba(255,255,255,.015) 40px,rgba(255,255,255,.015) 41px);pointer-events:none;}
.hero-banner::after{content:'';position:absolute;right:0;top:0;bottom:0;width:420px;background:linear-gradient(90deg,transparent,rgba(204,0,0,.12) 60%,rgba(204,0,0,.22));pointer-events:none;}
.hero-stripe{position:absolute;right:160px;top:0;bottom:0;width:6px;background:var(--Workplace);opacity:.7;clip-path:polygon(8px 0,100% 0,calc(100% - 8px) 100%,0 100%);transform:skewX(-2deg);}
.hero-stripe+.hero-stripe{right:148px;opacity:.35;width:3px;}
.hero-left{position:relative;z-index:2;display:flex;flex-direction:column;gap:4px;flex:1;}
.hero-logo-row{display:flex;align-items:center;gap:16px;margin-bottom:2px;}
.integratel-logo{display:flex;align-items:center;gap:10px;}
.integratel-mark{width:40px;height:40px;background:var(--Workplace);border-radius:8px;display:flex;align-items:center;justify-content:center;flex-shrink:0;box-shadow:0 2px 12px rgba(204,0,0,.4);}
.integratel-mark svg{width:24px;height:24px;}
.integratel-name{display:flex;flex-direction:column;line-height:1;}
.integratel-name span:first-child{font-family:var(--font-display);font-size:22px;font-weight:900;color:#fff;letter-spacing:1px;text-transform:uppercase;}
.integratel-name span:last-child{font-family:var(--font-display);font-size:10px;font-weight:600;color:rgba(255,255,255,.55);letter-spacing:3px;text-transform:uppercase;}
.hero-divider{width:1px;height:36px;background:rgba(255,255,255,.2);margin:0 4px;}
.Workplace-badge{display:flex;align-items:center;gap:8px;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.1);border-radius:8px;padding:6px 12px;}
.Workplace-badge-text{font-family:var(--font-display);font-size:18px;font-weight:900;color:#fff;letter-spacing:2px;text-transform:uppercase;}
.Workplace-badge-sub{font-size:9px;color:rgba(255,255,255,.45);letter-spacing:2px;text-transform:uppercase;margin-top:1px;}
.hero-title-row{display:flex;align-items:baseline;gap:12px;}
.hero-title{font-family:var(--font-display);font-size:28px;font-weight:900;color:#fff;letter-spacing:.5px;text-transform:uppercase;}
.hero-title span{color:var(--Workplace);}
.hero-sub{font-size:11.5px;color:rgba(255,255,255,.5);letter-spacing:.3px;}
.hero-right{position:relative;z-index:2;display:flex;align-items:center;gap:10px;flex-shrink:0;}
.printer-illustration{width:90px;height:70px;opacity:.85;filter:drop-shadow(0 4px 12px rgba(0,0,0,.4));}

/* ── Action bar ── */
.action-bar{background:var(--card);border-bottom:1px solid var(--border);padding:12px 32px;display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:10px;box-shadow:var(--shadow);}
.action-bar-left{display:flex;align-items:center;gap:8px;}
.action-bar-right{display:flex;align-items:center;gap:8px;}
.status-dot{width:8px;height:8px;border-radius:50%;background:var(--success);box-shadow:0 0 0 3px rgba(10,138,92,.2);animation:pulse-dot 2s infinite;}
@keyframes pulse-dot{0%,100%{box-shadow:0 0 0 3px rgba(10,138,92,.2);}50%{box-shadow:0 0 0 6px rgba(10,138,92,.05);}}
.status-label{font-size:11.5px;font-weight:700;color:var(--muted);letter-spacing:.3px;}
.sync-badge{font-size:10.5px;color:var(--muted);font-weight:600;white-space:nowrap;padding:4px 8px;background:var(--header);border:1px solid var(--border);border-radius:7px;font-family:var(--font-mono);}

/* ── Buttons ── */
.btn-primary{background:linear-gradient(135deg,var(--primary),var(--primary-light));color:#fff;border:none;border-radius:8px;padding:6px 13px;font-size:12px;font-weight:700;font-family:var(--font);cursor:pointer;display:inline-flex;align-items:center;gap:6px;transition:all .2s;white-space:nowrap;box-shadow:0 2px 6px rgba(10,61,143,.25);}
.btn-primary:hover{filter:brightness(1.1);box-shadow:0 3px 12px rgba(10,61,143,.35);}
.btn-primary:disabled{opacity:.6;cursor:not-allowed;box-shadow:none;}
.btn-ghost{background:transparent;color:var(--primary);border:1.5px solid var(--primary);border-radius:8px;padding:5px 12px;font-size:12px;font-weight:700;font-family:var(--font);cursor:pointer;display:inline-flex;align-items:center;gap:6px;transition:all .2s;}
.btn-ghost:hover{background:var(--primary);color:#fff;}
.btn-outline{background:transparent;border:1px solid var(--border);color:var(--muted);border-radius:8px;padding:7px 12px;font-size:12px;font-family:var(--font);cursor:pointer;display:inline-flex;align-items:center;gap:5px;transition:all .15s;}
.btn-outline:hover{border-color:var(--primary);color:var(--primary);}
.btn-sm{padding:5px 9px;font-size:11.5px;border-radius:7px;border:1px solid var(--border);background:transparent;color:var(--muted);cursor:pointer;display:inline-flex;align-items:center;gap:4px;transition:all .15s;white-space:nowrap;font-family:var(--font);}
.btn-sm:hover{border-color:var(--primary);color:var(--primary);}
.btn-sm.danger:hover{border-color:var(--danger);color:var(--danger);}
.btn-sm.success-h:hover{border-color:var(--success);color:var(--success);}
.btn-sm.warn-h:hover{border-color:var(--warning);color:var(--warning);}
@keyframes spin{to{transform:rotate(360deg);}}
.spin-anim{animation:spin .7s linear infinite;}

/* ── Tabs ── */
.tab-container{background:var(--card);border-bottom:1px solid var(--border);padding:0 32px;}
.tab-strip{display:flex;gap:0;border-bottom:none;margin-bottom:0;}
.tab-btn{padding:13px 24px;font-size:13px;font-weight:700;font-family:var(--font);border:none;background:transparent;color:var(--muted);cursor:pointer;border-bottom:3px solid transparent;margin-bottom:-1px;transition:all .2s;display:inline-flex;align-items:center;gap:8px;border-radius:0;letter-spacing:.2px;}
.tab-btn:hover{color:var(--text);background:var(--row-hover);}
.tab-btn.active{color:var(--primary);border-bottom-color:var(--primary);}
.tab-btn.vip-tab.active{color:var(--gold);border-bottom-color:var(--gold);}
.tab-badge{display:inline-flex;align-items:center;justify-content:center;min-width:22px;height:20px;border-radius:10px;font-size:10px;font-weight:800;padding:0 6px;background:var(--border);color:var(--muted);font-family:var(--font-mono);}
.tab-btn.active .tab-badge{background:var(--primary-glow);color:var(--primary);}
.tab-btn.vip-tab.active .tab-badge{background:rgba(201,134,10,.15);color:var(--gold);}
.tab-btn.vip-tab .tab-badge{background:rgba(201,134,10,.1);color:var(--gold);}

/* ── FILTER BAR ── */
.filter-bar{
  background:var(--header);border-bottom:1px solid var(--border);
  padding:10px 32px;display:flex;align-items:center;gap:10px;flex-wrap:wrap;
}
.search-box{position:relative;flex:1;min-width:220px;max-width:380px;}
.search-box i{position:absolute;left:11px;top:50%;transform:translateY(-50%);color:var(--muted);font-size:13px;pointer-events:none;}
.search-input{
  width:100%;padding:8px 12px 8px 34px;border:1.5px solid var(--border);border-radius:9px;
  font-size:12.5px;font-family:var(--font);background:var(--card);color:var(--text);
  outline:none;transition:border-color .2s,box-shadow .2s;
}
.search-input:focus{border-color:var(--primary);box-shadow:0 0 0 3px var(--primary-glow);}
.search-input::placeholder{color:var(--muted);}
.filter-select{
  background:var(--card);border:1.5px solid var(--border);color:var(--text);
  border-radius:9px;padding:8px 11px;font-size:12px;font-family:var(--font);
  outline:none;cursor:pointer;transition:border-color .2s;
}
.filter-select:focus{border-color:var(--primary);}
.filter-clear{background:transparent;border:1px solid var(--border);color:var(--muted);border-radius:8px;padding:7px 12px;font-size:12px;font-family:var(--font);cursor:pointer;display:inline-flex;align-items:center;gap:5px;transition:all .15s;white-space:nowrap;}
.filter-clear:hover{border-color:var(--danger);color:var(--danger);}
.filter-results{font-size:12px;color:var(--muted);font-family:var(--font-mono);white-space:nowrap;margin-left:auto;}

/* ── Table ── */
.table-container{background:var(--card);border-bottom:1px solid var(--border);}
.main-panel{display:flex;overflow:hidden;}
.tbl-side{flex:1;min-width:0;display:flex;flex-direction:column;}
.tbl-scroll{overflow-x:auto;overflow-y:auto;max-height:calc(100vh - 190px);}
.tbl{width:100%;border-collapse:collapse;font-size:12.5px;}
.tbl thead th{font-size:10px;text-transform:uppercase;letter-spacing:.6px;color:var(--muted);font-weight:800;font-family:var(--font-display);padding:11px 14px;background:var(--header);border-bottom:2px solid var(--border);white-space:nowrap;position:sticky;top:0;z-index:5;cursor:pointer;user-select:none;}
.tbl thead th:hover{color:var(--text);}
.tbl thead th.sorted{color:var(--primary);}
.tbl tbody td{padding:11px 14px;border-bottom:1px solid var(--border);color:var(--text);vertical-align:middle;}
.tbl tbody tr:last-child td{border-bottom:none;}
.tbl tbody tr{cursor:pointer;transition:background .1s;}
.tbl tbody tr:hover td{background:var(--row-hover);}
.tbl tbody tr.row-active td{background:var(--primary-glow);border-left:3px solid var(--primary);}
.tr-vip td{background:var(--gold-bg)!important;}
.tr-vip:hover td{background:rgba(201,134,10,.12)!important;}
.tr-vip.row-active td{background:rgba(201,134,10,.16)!important;border-left:3px solid var(--gold)!important;}
.tr-vip td:first-child::before{content:'⭐ ';font-size:11px;}
.tr-urgente:not(.tr-vip) td:first-child{border-left:3px solid var(--danger)!important;}
.tbl-foot{padding:12px 20px;border-top:1px solid var(--border);display:flex;align-items:center;justify-content:space-between;flex-wrap:wrap;gap:8px;background:var(--header);}
.tbl-info{font-size:11.5px;color:var(--muted);font-family:var(--font-mono);}
.pagination{display:flex;gap:4px;}
.pg-btn{background:var(--card);border:1px solid var(--border);color:var(--text);border-radius:7px;padding:4px 11px;font-size:12px;cursor:pointer;transition:.15s;font-family:var(--font-mono);}
.pg-btn:hover,.pg-btn.active{background:var(--primary);color:#fff;border-color:var(--primary);}
.pg-btn:disabled{opacity:.35;cursor:not-allowed;}

/* ── Table action buttons ── */
.btn-print{display:inline-flex;align-items:center;gap:5px;border-radius:8px;padding:5px 10px;font-size:11px;font-weight:800;font-family:var(--font);background:linear-gradient(135deg,var(--primary),var(--primary-light));color:#fff;border:none;cursor:pointer;transition:all .2s;white-space:nowrap;box-shadow:0 2px 6px rgba(10,61,143,.25);letter-spacing:.3px;}
.btn-print:hover{filter:brightness(1.1);box-shadow:0 3px 10px rgba(10,61,143,.35);}
.btn-print:disabled{opacity:.5;cursor:not-allowed;}
.btn-mailbox{display:inline-flex;align-items:center;gap:4px;border-radius:7px;padding:4px 9px;font-size:11px;font-weight:700;font-family:var(--font);border:none;cursor:pointer;transition:all .2s;white-space:nowrap;}
.btn-mailbox.pendiente{background:rgba(192,57,43,.1);color:var(--danger);border:1px solid rgba(192,57,43,.25);}
.btn-mailbox.pendiente:hover{background:var(--danger);color:#fff;}
.btn-mailbox.eliminado{background:rgba(10,138,92,.1);color:var(--success);border:1px solid rgba(10,138,92,.25);cursor:default;}

/* ── Pills ── */
.pill{display:inline-block;padding:3px 10px;border-radius:20px;font-size:11px;font-weight:700;white-space:nowrap;line-height:1.6;font-family:var(--font);}
.p-blue{background:rgba(10,61,143,.1);color:var(--primary);}
.p-green{background:rgba(10,138,92,.12);color:var(--success);}
.p-yellow{background:rgba(201,135,0,.12);color:var(--warning);}
.p-red{background:rgba(192,57,43,.12);color:var(--danger);}
.p-gray{background:rgba(90,113,148,.1);color:var(--muted);}
.p-orange{background:rgba(214,90,0,.12);color:#d45a00;}
.p-gold{background:rgba(201,134,10,.15);color:var(--gold);}
.pages-badge{display:inline-flex;align-items:center;gap:4px;background:linear-gradient(135deg,rgba(10,138,92,.12),rgba(10,138,92,.07));color:var(--success);border-radius:8px;padding:4px 10px;font-size:12px;font-weight:800;font-family:var(--font-mono);border:1px solid rgba(10,138,92,.2);}
.ft-icon{font-size:20px;}
.ft-pdf{color:#c0392b;}.ft-word{color:#0a3d8f;}.ft-excel{color:#0a8a5c;}.ft-ppt{color:#d45a00;}.ft-image{color:#6c3db5;}.ft-text,.ft-other{color:var(--muted);}

/* ── Toasts ── */
.toast-wrap{position:fixed;bottom:24px;right:24px;z-index:9999;display:flex;flex-direction:column;gap:8px;}
.toast-item{background:var(--card);border:1px solid var(--border);color:var(--text);border-radius:12px;padding:13px 18px;font-size:13px;font-weight:500;box-shadow:var(--shadow-md);display:flex;align-items:center;gap:10px;min-width:300px;animation:toastIn .25s ease;font-family:var(--font);}
.toast-item.ok{border-left:4px solid var(--success);}
.toast-item.err{border-left:4px solid var(--danger);}
.toast-item.inf{border-left:4px solid var(--primary);}
@keyframes toastIn{from{opacity:0;transform:translateX(24px);}to{opacity:1;transform:translateX(0);}}

/* ── Modals ── */
.modal-bg{display:none;position:fixed;inset:0;background:rgba(5,15,35,.65);z-index:1050;align-items:center;justify-content:center;backdrop-filter:blur(4px);}
.modal-bg.open{display:flex;}
.modal-box{background:var(--card);border:1px solid var(--border);border-radius:16px;padding:30px;width:100%;max-width:480px;box-shadow:var(--shadow-lg);animation:mIn .2s ease;}
.modal-box.wide{max-width:620px;}
@keyframes mIn{from{opacity:0;transform:scale(.95);}to{opacity:1;transform:scale(1);}}
.modal-box h5{font-size:17px;font-weight:800;font-family:var(--font-display);color:var(--text);margin-bottom:18px;display:flex;align-items:center;gap:9px;letter-spacing:.3px;}
.modal-box p{font-size:13px;color:var(--muted);margin-bottom:20px;line-height:1.6;}
.m-actions{margin-top:20px;display:flex;gap:10px;justify-content:flex-end;}
.btn-mcancel{background:transparent;border:1px solid var(--border);color:var(--muted);border-radius:9px;padding:8px 18px;font-size:13px;cursor:pointer;font-family:var(--font);}
.btn-mcancel:hover{border-color:var(--text);color:var(--text);}
.btn-mclose{width:28px;height:28px;border-radius:7px;border:1px solid rgba(255,255,255,.2);background:rgba(255,255,255,.1);color:rgba(255,255,255,.7);cursor:pointer;font-size:16px;display:flex;align-items:center;justify-content:center;transition:all .2s;}
.btn-mclose:hover{background:rgba(255,255,255,.2);color:#fff;}
.btn-mconfirm{background:var(--Workplace);color:#fff;border:none;border-radius:9px;padding:8px 20px;font-size:13px;font-weight:700;cursor:pointer;font-family:var(--font);}
.btn-mconfirm:hover{background:var(--Workplace-dark);}

/* ── Code modal ── */
.code-modal-bg{display:none;position:fixed;inset:0;background:rgba(5,15,35,.7);z-index:2000;align-items:center;justify-content:center;backdrop-filter:blur(5px);}
.code-modal-bg.open{display:flex;}
.code-modal-box{background:var(--card);border:1px solid var(--border);border-radius:20px;width:100%;max-width:400px;box-shadow:var(--shadow-lg);animation:mIn .2s ease;overflow:hidden;}
.code-modal-head{padding:18px 24px;background:linear-gradient(135deg,#051530,#0a2560);border-bottom:2px solid var(--Workplace);display:flex;align-items:center;justify-content:space-between;font-weight:800;font-size:15px;color:#fff;font-family:var(--font-display);letter-spacing:.5px;}
.code-modal-body{padding:24px;}
.code-input{width:100%;text-align:center;font-size:36px;font-weight:900;letter-spacing:12px;padding:16px;border:2px solid var(--border);border-radius:12px;background:var(--input-bg);color:var(--text);outline:none;transition:border-color .2s;font-family:var(--font-mono);}
.code-input:focus{border-color:var(--primary);box-shadow:0 0 0 3px var(--primary-glow);}
.code-input.shake{animation:shk .4s ease;border-color:var(--danger);}
.code-hint{font-size:12px;color:var(--muted);text-align:center;margin-top:10px;line-height:1.6;}
.code-actions{display:flex;gap:8px;margin-top:18px;}
.btn-code-cancel{flex:1;background:transparent;border:1px solid var(--border);color:var(--muted);border-radius:9px;padding:10px;font-size:13px;cursor:pointer;font-family:var(--font);}
.btn-code-cancel:hover{border-color:var(--text);color:var(--text);}
.btn-code-ok{flex:2;background:linear-gradient(135deg,var(--primary),var(--primary-light));color:#fff;border:none;border-radius:9px;padding:10px;font-size:13px;font-weight:700;cursor:pointer;font-family:var(--font);box-shadow:0 2px 8px rgba(10,61,143,.3);}
.btn-code-ok:hover{filter:brightness(1.1);}
.btn-code-ok:disabled{opacity:.55;cursor:not-allowed;}

/* ── Drawer ── */
.drawer{width:0;overflow:hidden;border-left:1px solid transparent;transition:width .3s cubic-bezier(.4,0,.2,1),border-color .3s;flex-shrink:0;display:flex;flex-direction:column;}
.drawer.open{width:360px;border-left-color:var(--border);}
.drawer-header{padding:14px 18px;background:linear-gradient(135deg,#051530,#0a2560);border-bottom:2px solid var(--Workplace);display:flex;align-items:center;justify-content:space-between;flex-shrink:0;}
.drawer-header h6{font-size:11.5px;font-weight:700;color:rgba(255,255,255,.9);margin:0;font-family:var(--font-mono);max-width:280px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap;}
.drawer-body{flex:1;overflow-y:auto;padding:0;}
.drawer-section{padding:14px 16px;border-bottom:1px solid var(--border);}
.drawer-section:last-child{border-bottom:none;}
.ds-title{font-size:9.5px;text-transform:uppercase;letter-spacing:.8px;font-weight:800;font-family:var(--font-display);margin-bottom:10px;display:flex;align-items:center;gap:6px;}
.ds-title.hw{color:var(--success);}.ds-title.sec{color:var(--primary);}.ds-title.gen{color:var(--muted);}
.ds-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;}
.ds-item{background:var(--input-bg);border-radius:8px;padding:9px 11px;border:1px solid var(--border);}
.ds-item.full{grid-column:1/-1;}
.ds-key{font-size:9px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted);font-weight:800;font-family:var(--font-display);margin-bottom:4px;}
.ds-val{font-size:12px;font-weight:600;color:var(--text);word-break:break-all;line-height:1.4;}
.ds-val.mono{font-family:var(--font-mono);font-size:11px;}
.ds-val.dim{color:var(--muted);font-weight:400;}
.drawer-close{background:rgba(255,255,255,.1);border:1px solid rgba(255,255,255,.15);color:rgba(255,255,255,.7);cursor:pointer;padding:5px 8px;border-radius:7px;display:flex;align-items:center;transition:.15s;}
.drawer-close:hover{background:rgba(255,255,255,.2);color:#fff;}
.drawer-actions{display:flex;flex-direction:column;gap:6px;padding:14px 16px;border-bottom:1px solid var(--border);}
.drawer-actions .d-row{display:flex;gap:6px;}

/* ── VIP ── */
.vip-list{display:flex;flex-direction:column;gap:6px;max-height:280px;overflow-y:auto;margin-bottom:16px;}
.vip-row{display:flex;align-items:center;gap:10px;background:var(--input-bg);border:1px solid var(--border);border-radius:10px;padding:10px 14px;}
.vip-row-info{flex:1;}
.vip-row-name{font-size:13px;font-weight:700;color:var(--text);}
.vip-row-email{font-size:11.5px;color:var(--muted);font-family:var(--font-mono);}
.vip-add-form{display:flex;gap:8px;flex-wrap:wrap;}
.vip-input{background:var(--input-bg);border:1px solid var(--border);color:var(--text);border-radius:9px;padding:8px 13px;font-size:12.5px;flex:1;min-width:160px;outline:none;font-family:var(--font);}
.vip-input:focus{border-color:var(--gold);box-shadow:0 0 0 3px rgba(201,134,10,.12);}

/* ── Misc ── */
*::-webkit-scrollbar{width:5px;height:5px;}
*::-webkit-scrollbar-track{background:transparent;}
*::-webkit-scrollbar-thumb{background:var(--border);border-radius:3px;}
*::-webkit-scrollbar-thumb:hover{background:var(--muted);}
@keyframes shk{0%,100%{transform:translateX(0);}20%,60%{transform:translateX(-6px);}40%,80%{transform:translateX(6px);}}
@keyframes fadeUp{from{opacity:0;transform:translateY(10px);}to{opacity:1;transform:translateY(0);}}
.pg>*{animation:fadeUp .35s ease both;}
.pg>*:nth-child(1){animation-delay:.05s;}
.pg>*:nth-child(2){animation-delay:.1s;}
.pg>*:nth-child(3){animation-delay:.15s;}
.empty-state{text-align:center;padding:70px 20px;color:var(--muted);}
.empty-state i{font-size:40px;display:block;margin-bottom:14px;opacity:.3;}
.empty-state p{font-size:13px;font-weight:600;}
.Workplace-ribbon{background:linear-gradient(90deg,#0a0a0a,#1a0000,#0a0a0a);border-top:2px solid var(--Workplace);padding:8px 32px;display:flex;align-items:center;justify-content:space-between;}
.Workplace-ribbon-text{font-family:var(--font-display);font-size:10px;font-weight:700;color:rgba(255,255,255,.35);letter-spacing:2px;text-transform:uppercase;}
.Workplace-ribbon-logo{font-family:var(--font-display);font-size:14px;font-weight:900;color:var(--Workplace);letter-spacing:3px;text-transform:uppercase;}
#printFrame{display:none;}
</style>
</head>
<body>
<style>
.itsm-topbar-pq{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:10px 24px;background:var(--card,#1a1a2e);border-bottom:1px solid var(--border,rgba(255,255,255,.1));position:sticky;top:0;z-index:999;}
.itsm-back-pq{display:inline-flex;align-items:center;gap:6px;font-size:12px;font-weight:600;color:#94a3b8;background:rgba(255,255,255,.06);border:1px solid rgba(255,255,255,.12);border-radius:20px;padding:5px 12px;text-decoration:none;transition:color .2s;}
.itsm-back-pq:hover{color:#60a5fa;}
.itsm-bc-pq{display:flex;align-items:center;gap:6px;font-size:12px;color:#94a3b8;}
.itsm-bc-pq a{color:#94a3b8;text-decoration:none;font-weight:500;}
.itsm-bc-pq a:hover{color:#60a5fa;}
.itsm-bc-pq .sep{opacity:.4;}
.itsm-bc-pq .current{color:#e2e8f0;font-weight:700;}
</style>
<div class="itsm-topbar-pq">
  <div style="display:flex;align-items:center;gap:12px;">
    <a href="/itsm" class="itsm-back-pq"><i class="bi bi-arrow-left"></i> ITSM</a>
    <div class="itsm-bc-pq">
      <a href="/administracion"><i class="bi bi-shield-lock-fill"></i> Admin</a>
      <span class="sep">/</span>
      <a href="/itsm">ITSM</a>
      <span class="sep">/</span>
      <span class="current"><i class="bi bi-printer-fill"></i> Cola de Impresión</span>
    </div>
  </div>
  <button onclick="toggleTheme()" style="display:flex;align-items:center;gap:6px;background:none;border:none;cursor:pointer;padding:4px 8px;border-radius:8px;">
    <div id="themeTrack" style="width:32px;height:18px;border-radius:9px;background:#cbd5e1;position:relative;transition:background .3s;">
      <div id="themeKnob" style="width:14px;height:14px;border-radius:50%;background:#fff;position:absolute;top:2px;left:2px;transition:transform .3s;"></div>
    </div>
    <i id="toggleIcon" class="bi bi-moon-stars-fill" style="font-size:14px;color:#94a3b8;"></i>
    <span id="toggleLabel" style="font-size:12px;color:#94a3b8;font-weight:500;">Oscuro</span>
  </button>
</div>
<div class="pg">

  <!-- ── Action bar ── -->
  <div class="action-bar">
    <div class="action-bar-left">
      <div class="status-dot"></div>
      <span class="status-label">SISTEMA ACTIVO</span>
    </div>
    <div class="action-bar-right">
      <button class="btn-ghost" id="btnRefresh" onclick="loadAll()">
        <i class="bi bi-arrow-clockwise" id="refreshIcon"></i> Actualizar
      </button>
      <button class="btn-primary" id="btnFetch" onclick="fetchEmails()">
        <i class="bi bi-envelope-arrow-down" id="fetchIcon"></i>
        <span id="fetchLabel">Buscar correos</span>
      </button>
      <span class="sync-badge" id="autoFetchCountdown">Sync en 3:00</span>
    </div>
  </div>

  <!-- ── Tabs ── -->
  <div class="tab-container">
    <div class="tab-strip">
      <button class="tab-btn active" id="tabAll" onclick="switchTab('all')">
        <i class="bi bi-list-ul"></i> Todos <span class="tab-badge" id="badgeAll">0</span>
      </button>
    </div>
  </div>

  <!-- ── BARRA DE BÚSQUEDA ── -->
  <div class="filter-bar">
    <div class="search-box">
      <i class="bi bi-search"></i>
      <input type="text" class="search-input" id="searchInput"
             placeholder="Buscar por nombre de archivo…"
             oninput="debounceSearch()">
    </div>
    <span class="filter-results" id="filterResults"></span>
  </div>

  <!-- ── Table ── -->
  <div class="table-container">
    <div class="main-panel">
      <div class="tbl-side">
        <div class="tbl-scroll">
          <table class="tbl">
            <thead>
              <tr>
                <th onclick="sortBy('id')" style="width:40px;"># <span class="sort-icon">⇅</span></th>
                <th onclick="sortBy('email_from_name')">REMITENTE <span class="sort-icon">⇅</span></th>
                <th onclick="sortBy('file_original')">ARCHIVO <span class="sort-icon">⇅</span></th>
                <th onclick="sortBy('num_pages')" style="width:70px;">HOJAS <span class="sort-icon">⇅</span></th>
                <th onclick="sortBy('status')">ESTADO <span class="sort-icon">⇅</span></th>
                <th>BUZÓN</th>
                <th style="width:160px;">ACCIONES</th>
              </tr>
            </thead>
            <tbody id="tableBody">
              <tr><td colspan="7" class="empty-state"><i class="bi bi-arrow-repeat spin-anim"></i><p>Cargando cola...</p></td></tr>
            </tbody>
          </table>
        </div>
        <div class="tbl-foot">
          <span class="tbl-info" id="tableInfo">—</span>
          <div class="pagination" id="pagination"></div>
        </div>
      </div>

      <!-- Drawer -->
      <div class="drawer" id="drawer">
        <div class="drawer-header">
          <h6 id="drawerFileName">—</h6>
          <button class="drawer-close" onclick="closeDrawer()"><i class="bi bi-x-lg"></i></button>
        </div>
        <div class="drawer-body" id="drawerBody"></div>
      </div>
    </div>
  </div>


</div><!-- /.pg -->

<!-- Modal ya fue impreso -->
<div class="modal-bg" id="printedModal">
  <div class="modal-box" style="max-width:400px;text-align:center;">
    <div style="width:64px;height:64px;background:rgba(10,138,92,.12);border-radius:50%;display:flex;align-items:center;justify-content:center;margin:0 auto 16px;border:2px solid rgba(10,138,92,.25);">
      <i class="bi bi-check-circle-fill" style="font-size:30px;color:var(--success);"></i>
    </div>
    <h5 style="justify-content:center;margin-bottom:8px;">Documento ya impreso</h5>
    <p id="printedModalMsg" style="margin-bottom:6px;"></p>
    <p id="printedModalDate" style="font-size:12px;color:var(--muted);margin-bottom:0;"></p>
    <div class="m-actions" style="justify-content:center;">
      <button class="btn-mcancel" onclick="document.getElementById('printedModal').classList.remove('open')">Cerrar</button>
    </div>
  </div>
</div>

<!-- Modal eliminar -->
<div class="modal-bg" id="confirmModal">
  <div class="modal-box">
    <h5><i class="bi bi-trash" style="color:var(--danger)"></i> Eliminar documento</h5>
    <p id="confirmMsg">¿Estás seguro?</p>
    <div class="m-actions">
      <button class="btn-mcancel" onclick="closeConfirm()">Cancelar</button>
      <button class="btn-mconfirm" onclick="doConfirm()">Eliminar</button>
    </div>
  </div>
</div>

<!-- Modal VIP -->
<div class="modal-bg" id="vipModal">
  <div class="modal-box wide">
    <h5><i class="bi bi-star-fill" style="color:var(--gold)"></i> Gestión de Usuarios VIP</h5>
    <div class="vip-list" id="vipList"><div style="text-align:center;padding:20px;color:var(--muted);">Cargando...</div></div>
    <div class="vip-add-form">
      <input type="email" class="vip-input" id="vipEmail" placeholder="correo@empresa.com" autocomplete="off">
      <input type="text"  class="vip-input" id="vipName"  placeholder="Nombre (opcional)">
      <button class="btn-primary" onclick="addVip()" style="padding:8px 16px;"><i class="bi bi-star"></i> Agregar VIP</button>
    </div>
    <div class="m-actions"><button class="btn-mcancel" onclick="closeVipModal()">Cerrar</button></div>
  </div>
</div>

<!-- Modal código de seguridad -->
<div class="code-modal-bg" id="codeModal">
  <div class="code-modal-box">
    <div class="code-modal-head">
      <span style="display:flex;align-items:center;gap:9px;">
        <i class="bi bi-shield-lock-fill" style="font-size:17px;"></i>
        Código de impresión
      </span>
      <button class="btn-mclose" onclick="closeCodeModal()"><i class="bi bi-x"></i></button>
    </div>
    <div class="code-modal-body">
      <div style="font-size:12px;color:var(--muted);text-align:center;margin-bottom:16px;">
        Ingresa el código que recibiste en tu correo electrónico
      </div>
      <div style="font-size:10px;text-transform:uppercase;letter-spacing:.5px;color:var(--muted);font-weight:700;text-align:center;margin-bottom:6px;" id="codeModalInfo"></div>
      <input type="text" class="code-input" id="codeInput"
             maxlength="6" placeholder="_ _ _ _"
             oninput="this.value=this.value.replace(/\D/g,'')">
      <div class="code-hint">
        El código fue enviado automáticamente al correo del remitente.<br>
        <strong>De un solo uso</strong> — el archivo se elimina 2 min después de imprimir.
      </div>
      <div class="code-actions">
        <button class="btn-code-cancel" onclick="closeCodeModal()">Cancelar</button>
        <button class="btn-code-ok" id="btnCodeOk" onclick="submitCode()">
          <i class="bi bi-printer-fill"></i> 🖨️ Imprimir
        </button>
      </div>
    </div>
  </div>
</div>

<iframe id="printFrame" style="display:none;"></iframe>
<div class="toast-wrap" id="toastWrap"></div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>

<script>
// ── State ─────────────────────────────────────────────────
let currentPage=1, currentTab='all', searchTimer=null;
let sortField='queued_at', sortDir='desc';
let activeRowId=null, confirmCb=null;
// Email del usuario autenticado (viene por URL param desde herramientas)
const _REPORTER_EMAIL = new URLSearchParams(window.location.search).get('reporter') || '';

// ── Utils ─────────────────────────────────────────────────
function toast(msg,type='inf',dur=4500){
  const icons={ok:'bi-check-circle-fill',err:'bi-x-circle-fill',inf:'bi-info-circle-fill'};
  const clrs ={ok:'var(--success)',err:'var(--danger)',inf:'var(--info)'};
  const el=document.createElement('div');
  el.className=`toast-item ${type}`;
  el.innerHTML=`<i class="bi ${icons[type]}" style="color:${clrs[type]};font-size:18px;flex-shrink:0;"></i><span>${msg}</span>`;
  document.getElementById('toastWrap').appendChild(el);
  setTimeout(()=>{el.style.cssText='opacity:0;transform:translateX(20px);transition:.3s;';setTimeout(()=>el.remove(),300);},dur);
}
const fmtDate=d=>{if(!d)return'—';const dt=new Date(d);return isNaN(dt)?d:dt.toLocaleString('es-PE',{day:'2-digit',month:'2-digit',year:'numeric',hour:'2-digit',minute:'2-digit'});};
const fmtSize=b=>{if(!b)return'—';if(b<1024)return b+'B';if(b<1048576)return(b/1024).toFixed(1)+'KB';return(b/1048576).toFixed(1)+'MB';};
const n=v=>(+v||0).toLocaleString('es-PE');
const esc=s=>(s||'').toString().replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;');
const val=s=>s?esc(s):'<span style="color:var(--muted);font-weight:400">—</span>';

const STATUS_MAP={pendiente:{cls:'p-yellow',lbl:'⏳ Pendiente'},imprimiendo:{cls:'p-blue',lbl:'🖨️ Imprimiendo'},completado:{cls:'p-green',lbl:'✅ Completado'},cancelado:{cls:'p-gray',lbl:'⛔ Cancelado'},error:{cls:'p-red',lbl:'❌ Error'}};
const PRIO_MAP={urgente:{cls:'p-red',lbl:'🔴 Urgente'},alta:{cls:'p-orange',lbl:'🟠 Alta'},normal:{cls:'p-yellow',lbl:'🟡 Normal'},baja:{cls:'p-gray',lbl:'⚪ Baja'}};
const FT_MAP={pdf:{icon:'bi-file-earmark-pdf',cls:'ft-pdf',lbl:'PDF'},word:{icon:'bi-file-earmark-word',cls:'ft-word',lbl:'Word'},excel:{icon:'bi-file-earmark-excel',cls:'ft-excel',lbl:'Excel'},powerpoint:{icon:'bi-file-earmark-ppt',cls:'ft-ppt',lbl:'PPT'},image:{icon:'bi-file-image',cls:'ft-image',lbl:'Imagen'},text:{icon:'bi-file-text',cls:'ft-text',lbl:'Texto'},other:{icon:'bi-file-earmark',cls:'ft-other',lbl:'Otro'}};
const statusBadge=s=>{const m=STATUS_MAP[s]||{cls:'p-gray',lbl:s||'—'};return`<span class="pill ${m.cls}">${m.lbl}</span>`;};
const prioBadge=p=>{const m=PRIO_MAP[p]||{cls:'p-gray',lbl:p||'—'};return`<span class="pill ${m.cls}">${m.lbl}</span>`;};
const ftIcon=t=>{const m=FT_MAP[t]||FT_MAP.other;return`<i class="bi ${m.icon} ft-icon ${m.cls}"></i>`;};
const ftBadge=t=>{const m=FT_MAP[t]||FT_MAP.other;return`<span class="pill p-gray" style="font-size:10px;">${m.lbl}</span>`;};

// ── Botón buzón ───────────────────────────────────────────
function mailboxButton(r){
  if(+r.email_deleted===1)
    return`<button class="btn-mailbox eliminado" disabled><i class="bi bi-check-circle-fill"></i> Eliminado</button>`;
  return`<button class="btn-mailbox pendiente" onclick="event.stopPropagation();deleteMailbox(${r.id})" id="mbBtn_${r.id}"><i class="bi bi-envelope-x-fill"></i> Del buzón</button>`;
}

// ── Búsqueda ──────────────────────────────────────────────
function debounceSearch(){
  clearTimeout(searchTimer);
  searchTimer=setTimeout(()=>loadData(1),380);
}
function clearFilters(){
  document.getElementById('searchInput').value='';
  loadData(1);
}

// ── Tabla ─────────────────────────────────────────────────
async function loadData(page=1){
  currentPage=page;
  const search = document.getElementById('searchInput').value.trim();

  let url=`/api/print-queue/data?page=${page}&limit=50&sort=${sortField}&dir=${sortDir}&tab=${currentTab}`;
  if(_REPORTER_EMAIL) url+=`&reporter=${encodeURIComponent(_REPORTER_EMAIL)}`;
  if(search) url+=`&search=${encodeURIComponent(search)}`;

  try{
    const j=await fetch(url).then(r=>r.json());
    if(!j.success){toast('Error cargando datos','err');return;}
    renderTable(j.data);
    renderPagination(j.pagination);
    const{page:p,limit:l,total}=j.pagination;
    const from=(p-1)*l+1,to=Math.min(p*l,total);
    document.getElementById('tableInfo').textContent=total?`${n(from)}–${n(to)} de ${n(total)} documentos`:'Sin resultados';
    // Contador de resultados filtrados
    const hasFilter=search;
    document.getElementById('filterResults').textContent=hasFilter?`${n(total)} resultado${total!==1?'s':''}`:'' ;
    updateActiveBadge(total);
  }catch(e){console.error(e);}
}

async function updateBadges(){
  try{
    const _rp = _REPORTER_EMAIL ? `&reporter=${encodeURIComponent(_REPORTER_EMAIL)}` : '';
    const[all,vip]=await Promise.all([
      fetch(`/api/print-queue/data?page=1&limit=1&tab=all${_rp}`).then(r=>r.json()),
      fetch(`/api/print-queue/data?page=1&limit=1&tab=vip${_rp}`).then(r=>r.json())
    ]);
    const ta=all.pagination?.total||0, tv=vip.pagination?.total||0;
    const bAll=document.getElementById('badgeAll');
    const bVip=document.getElementById('badgeVip');
    const bStd=document.getElementById('badgeStd');
    if(bAll) bAll.textContent=n(ta);
    if(bVip) bVip.textContent=n(tv);
    if(bStd) bStd.textContent=n(ta-tv);
    if(tv>0){const vb=document.querySelector('#tabVip .tab-badge');if(vb){vb.style.background='rgba(245,158,11,.15)';vb.style.color='var(--gold)';}}
  }catch(e){console.warn('updateBadges:',e.message);}
}
function updateActiveBadge(total){
  const id=currentTab==='vip'?'badgeVip':currentTab==='standard'?'badgeStd':'badgeAll';
  document.getElementById(id).textContent=n(total);
}

function renderTable(rows){
  const tbody=document.getElementById('tableBody');
  if(!rows.length){tbody.innerHTML=`<tr><td colspan="7" class="empty-state"><i class="bi bi-inbox"></i><p>Sin documentos con los filtros aplicados</p></td></tr>`;return;}
  tbody.innerHTML=rows.map(r=>{
    const isVip=+r.is_vip===1,isUrgent=r.priority==='urgente'&&r.status==='pendiente',isActive=r.id===activeRowId;
    const fname=r.file_original||r.file_name||'—';
    const fnameShort=fname.length>28?fname.substring(0,26)+'…':fname;
    const sender=r.email_from_name||r.email_from_addr||'—';
    const senderShort=sender.length>24?sender.substring(0,22)+'…':sender;
    const pagesCell=r.num_pages?`<span class="pages-badge"><i class="bi bi-file-earmark-text"></i> ${n(r.num_pages)}</span>`:`<span style="color:var(--muted);font-size:11px;">—</span>`;
    let actions='';
    if(r.status==='pendiente'){
      actions+=`<button class="btn-sm success-h" onclick="event.stopPropagation();changeStatus(${r.id},'imprimiendo')" title="Iniciar"><i class="bi bi-play-fill"></i></button>`;
      actions+=`<button class="btn-sm warn-h" onclick="event.stopPropagation();changeStatus(${r.id},'cancelado')" title="Cancelar"><i class="bi bi-x-circle"></i></button>`;
    }else if(r.status==='imprimiendo'){
      actions+=`<button class="btn-sm success-h" onclick="event.stopPropagation();changeStatus(${r.id},'completado')" title="Completado"><i class="bi bi-check-lg"></i></button>`;
      actions+=`<button class="btn-sm danger" onclick="event.stopPropagation();changeStatus(${r.id},'error')" title="Error"><i class="bi bi-exclamation-triangle"></i></button>`;
    }else if(r.status==='error'||r.status==='cancelado'){
      actions+=`<button class="btn-sm" onclick="event.stopPropagation();changeStatus(${r.id},'pendiente')" title="Reabrir"><i class="bi bi-arrow-counterclockwise"></i></button>`;
    }
    if(r.status==='completado')
      actions+=`<button class="btn-print" style="background:linear-gradient(135deg,var(--success),#0d9e6a);box-shadow:0 2px 6px rgba(10,138,92,.25);" onclick="event.stopPropagation();openPrintedModal(${r.id},'${esc(fname)}','${esc(r.printed_at||'')}')" title="Ya impreso"><i class="bi bi-check-circle-fill"></i> Impreso</button>`;
    else
      actions+=`<button class="btn-print" onclick="event.stopPropagation();openCodeModal(${r.id},'${esc(r.file_type)}')" title="Imprimir — requiere código"><i class="bi bi-printer-fill"></i> Imprimir</button>`;
    if(r.status!=='completado')
      actions+=`<button class="btn-sm danger" onclick="event.stopPropagation();confirmDelete(${r.id},'${esc(fname)}')" title="Eliminar"><i class="bi bi-trash"></i></button>`;
    const trCls=`${isVip?'tr-vip':''} ${isUrgent&&!isVip?'tr-urgente':''} ${isActive?'row-active':''}`.trim();
    return`<tr class="${trCls}" onclick="openDrawer(${JSON.stringify(r).replace(/'/g,'&#39;')})">
      <td style="font-family:monospace;font-size:11px;color:var(--muted);">${r.id}</td>
      <td>
        <div style="font-size:12.5px;font-weight:700;">${esc(senderShort)}${isVip?` <span class="pill p-gold" style="font-size:10px;padding:1px 6px;">⭐ VIP</span>`:''}</div>
        <div style="font-size:11px;color:var(--muted);">${esc(r.email_from_addr||'')}</div>
      </td>
      <td>
        <div style="display:flex;align-items:center;gap:7px;">${ftIcon(r.file_type)}
          <div>
            <div style="font-size:12px;font-weight:600;" title="${esc(fname)}">${esc(fnameShort)}</div>
            <div style="font-size:11px;color:var(--muted);">${fmtSize(r.file_size)}</div>
          </div>
        </div>
      </td>
      <td>${pagesCell}</td>
      <td>${statusBadge(r.status)}</td>
      <td>${mailboxButton(r)}</td>
      <td><div style="display:flex;gap:3px;flex-wrap:wrap;">${actions}</div></td>
    </tr>`;
  }).join('');
}

function renderPagination({page,pages}){
  const pg=document.getElementById('pagination');
  if(pages<=1){pg.innerHTML='';return;}
  let h=`<button class="pg-btn" onclick="loadData(${page-1})" ${page<=1?'disabled':''}>‹</button>`;
  const s=Math.max(1,page-2),e=Math.min(pages,page+2);
  if(s>1)h+=`<button class="pg-btn" onclick="loadData(1)">1</button>${s>2?'<span style="padding:0 4px;color:var(--muted);">…</span>':''}`;
  for(let i=s;i<=e;i++)h+=`<button class="pg-btn ${i===page?'active':''}" onclick="loadData(${i})">${i}</button>`;
  if(e<pages)h+=`${e<pages-1?'<span style="padding:0 4px;color:var(--muted);">…</span>':''}<button class="pg-btn" onclick="loadData(${pages})">${pages}</button>`;
  h+=`<button class="pg-btn" onclick="loadData(${page+1})" ${page>=pages?'disabled':''}>›</button>`;
  pg.innerHTML=h;
}

function sortBy(field){
  sortDir=sortField===field?(sortDir==='asc'?'desc':'asc'):'desc';sortField=field;
  document.querySelectorAll('.tbl thead th').forEach(th=>{th.classList.remove('sorted');const ic=th.querySelector('.sort-icon');if(ic)ic.textContent='⇅';});
  document.querySelectorAll('.tbl thead th[onclick]').forEach(th=>{if(th.getAttribute('onclick').includes(`'${field}'`)){th.classList.add('sorted');const ic=th.querySelector('.sort-icon');if(ic)ic.textContent=sortDir==='asc'?'↑':'↓';}});
  loadData(1);
}

function switchTab(tab){
  currentTab=tab;currentPage=1;
  document.querySelectorAll('.tab-btn').forEach(b=>b.classList.remove('active'));
  document.querySelectorAll('.tab-btn').forEach(b=>{if(b.getAttribute('onclick')===`switchTab('${tab}')`)b.classList.add('active');});
  loadData(1);
}

// ── Drawer ────────────────────────────────────────────────
function openDrawer(r){
  activeRowId=r.id;
  document.querySelectorAll('.tbl tbody tr').forEach(t=>t.classList.remove('row-active'));
  event?.currentTarget?.classList.add('row-active');
  document.getElementById('drawerFileName').textContent=r.file_original||r.file_name||'—';
  document.getElementById('drawerBody').innerHTML=buildDrawer(r);
  document.getElementById('drawer').classList.add('open');
}
function closeDrawer(){
  document.getElementById('drawer').classList.remove('open');
  activeRowId=null;
  document.querySelectorAll('.tbl tbody tr').forEach(t=>t.classList.remove('row-active'));
}

function buildDrawer(r){
  const fname=r.file_original||r.file_name||'—';
  const isVip=+r.is_vip===1;
  let actionBtns='';
  if(r.status==='pendiente')
    actionBtns+=`<button class="btn-primary" style="flex:1;justify-content:center;" onclick="changeStatus(${r.id},'imprimiendo')"><i class="bi bi-play-fill"></i> Iniciar impresión</button>
    <button class="btn-outline" onclick="changeStatus(${r.id},'cancelado')"><i class="bi bi-x-circle"></i></button>`;
  else if(r.status==='imprimiendo')
    actionBtns+=`<button class="btn-primary" style="flex:1;justify-content:center;background:var(--success);" onclick="changeStatus(${r.id},'completado')"><i class="bi bi-check-lg"></i> Completado</button>
    <button class="btn-outline" onclick="changeStatus(${r.id},'error')" style="color:var(--danger);border-color:var(--danger)"><i class="bi bi-exclamation-triangle"></i></button>`;
  const printBtn = r.status==='completado'
    ?`<button class="btn-print" style="flex:1;justify-content:center;padding:8px;background:linear-gradient(135deg,var(--success),#0d9e6a);box-shadow:0 2px 6px rgba(10,138,92,.25);" onclick="openPrintedModal(${r.id},'${esc(fname)}','${esc(r.printed_at||'')}')"><i class="bi bi-check-circle-fill"></i> Ya impreso</button>`
    :`<button class="btn-print" style="flex:1;justify-content:center;padding:8px;" onclick="openCodeModal(${r.id},'${esc(r.file_type)}')"><i class="bi bi-printer-fill"></i> Imprimir (requiere código)</button>`;
  const mbDrawer=+r.email_deleted===1
    ?`<button class="btn-mailbox eliminado" style="flex:1;justify-content:center;padding:8px;" disabled><i class="bi bi-check-circle-fill"></i> Correo eliminado del buzón</button>`
    :`<button class="btn-mailbox pendiente" style="flex:1;justify-content:center;padding:8px;" onclick="deleteMailbox(${r.id})" id="mbBtnD_${r.id}"><i class="bi bi-envelope-x-fill"></i> Eliminar del buzón</button>`;
  const pagesInfo=r.num_pages
    ?`<span class="pages-badge"><i class="bi bi-file-earmark-text"></i> ${n(r.num_pages)} páginas</span><div style="font-size:10.5px;color:var(--muted);margin-top:4px;">× ${r.copies||1} copias = <strong>${n(r.total_print_pages)}</strong> hojas totales</div>`
    :`<span style="color:var(--muted);font-size:12px;">No disponible para este tipo</span>`;
  return`
  ${isVip?`<div style="background:var(--gold-bg);border-bottom:1px solid rgba(245,158,11,.2);padding:10px 16px;display:flex;align-items:center;gap:8px;font-size:12px;font-weight:700;color:var(--gold);"><i class="bi bi-star-fill"></i> Usuario VIP — prioridad garantizada</div>`:''}
  <div class="drawer-actions">
    <div class="d-row">${actionBtns}</div>
    <div class="d-row">${printBtn}</div>
    <div class="d-row">${mbDrawer}</div>
  </div>
  <div class="drawer-section">
    <div class="ds-title gen"><i class="bi bi-envelope"></i> Correo origen</div>
    <div class="ds-grid">
      <div class="ds-item full"><div class="ds-key">Asunto</div><div class="ds-val">${val(r.email_subject)}</div></div>
      <div class="ds-item full"><div class="ds-key">Remitente</div><div class="ds-val">${val(r.email_from_name)}</div></div>
      <div class="ds-item full"><div class="ds-key">Email</div><div class="ds-val mono">${val(r.email_from_addr)}</div></div>
      <div class="ds-item"><div class="ds-key">Recibido</div><div class="ds-val dim">${fmtDate(r.received_at)}</div></div>
      <div class="ds-item"><div class="ds-key">Correo buzón</div><div class="ds-val">${+r.email_deleted===1?'<span class="pill p-green">✅ Eliminado</span>':'<span class="pill p-yellow">📬 En buzón</span>'}</div></div>
    </div>
  </div>
  <div class="drawer-section">
    <div class="ds-title hw"><i class="bi bi-file-earmark"></i> Archivo</div>
    <div class="ds-grid">
      <div class="ds-item full"><div class="ds-key">Nombre</div><div class="ds-val">${val(fname)}</div></div>
      <div class="ds-item"><div class="ds-key">Tipo</div><div class="ds-val">${ftBadge(r.file_type)}</div></div>
      <div class="ds-item"><div class="ds-key">Tamaño</div><div class="ds-val">${fmtSize(r.file_size)}</div></div>
      <div class="ds-item full"><div class="ds-key">Páginas</div><div class="ds-val">${pagesInfo}</div></div>
      <div class="ds-item"><div class="ds-key">Copias</div><div class="ds-val">${r.copies||1}</div></div>
      <div class="ds-item"><div class="ds-key">Prioridad</div><div class="ds-val">${prioBadge(r.priority)}</div></div>
    </div>
  </div>
  <div class="drawer-section">
    <div class="ds-title gen"><i class="bi bi-printer"></i> Estado impresión</div>
    <div class="ds-grid">
      <div class="ds-item full"><div class="ds-key">Estado</div><div class="ds-val">${statusBadge(r.status)}</div></div>
      <div class="ds-item"><div class="ds-key">Encolado</div><div class="ds-val dim">${fmtDate(r.queued_at)}</div></div>
      <div class="ds-item"><div class="ds-key">Impreso</div><div class="ds-val dim">${fmtDate(r.printed_at)}</div></div>
      ${r.printed_by?`<div class="ds-item full"><div class="ds-key">Impreso por</div><div class="ds-val dim">${val(r.printed_by)}</div></div>`:''}
      ${r.notes?`<div class="ds-item full"><div class="ds-key">Notas</div><div class="ds-val dim">${val(r.notes)}</div></div>`:''}
    </div>
  </div>`;
}

// ── Acciones ──────────────────────────────────────────────
async function changeStatus(id,status){
  try{
    const j=await fetch(`/api/print-queue/${id}/status`,{method:'PATCH',headers:{'Content-Type':'application/json'},body:JSON.stringify({status})}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    toast(`Documento #${id} → ${STATUS_MAP[status]?.lbl||status}`,'ok');
    await loadAll();if(activeRowId===id)closeDrawer();
  }catch(e){toast('Error: '+e.message,'err');}
}

function confirmDelete(id,fname){
  document.getElementById('confirmMsg').textContent=`¿Eliminar "${fname}" de la cola?`;
  confirmCb=async()=>{
    try{
      const j=await fetch(`/api/print-queue/${id}`,{method:'DELETE'}).then(r=>r.json());
      if(!j.success)throw new Error(j.error);
      toast('Documento eliminado','ok');await loadAll();if(activeRowId===id)closeDrawer();
    }catch(e){toast('Error: '+e.message,'err');}
  };
  document.getElementById('confirmModal').classList.add('open');
}
const doConfirm=()=>{closeConfirm();if(confirmCb)confirmCb();};
const closeConfirm=()=>{document.getElementById('confirmModal').classList.remove('open');confirmCb=null;};

// ── Modal "ya fue impreso" ────────────────────────────────
function openPrintedModal(id, fname, printedAt){
  document.getElementById('printedModalMsg').textContent = `"${fname}" ya fue enviado a la impresora.`;
  document.getElementById('printedModalDate').textContent = printedAt
    ? `Impreso el: ${fmtDate(printedAt)}`
    : 'El archivo local ya no está disponible.';
  document.getElementById('printedModal').classList.add('open');
}

// ── Modal código ───────────────────────────────────────────
let codeModalDocId=null,codeModalFileType=null;

function openCodeModal(id,fileType){
  codeModalDocId=id;codeModalFileType=fileType;
  document.getElementById('codeInput').value='';
  document.getElementById('codeInput').classList.remove('shake');
  document.getElementById('codeModalInfo').textContent=`Doc #${id}`;
  document.getElementById('codeModal').classList.add('open');
  setTimeout(()=>document.getElementById('codeInput').focus(),150);
}
function closeCodeModal(){
  document.getElementById('codeModal').classList.remove('open');
  codeModalDocId=null;codeModalFileType=null;
}
document.addEventListener('DOMContentLoaded',()=>{
  document.getElementById('codeInput')?.addEventListener('keydown',e=>{if(e.key==='Enter')submitCode();});
  document.getElementById('codeModal')?.addEventListener('click',e=>{if(e.target===document.getElementById('codeModal'))closeCodeModal();});
});

async function submitCode(){
  const code=document.getElementById('codeInput').value.trim();
  if(!code){shakeCode();return;}
  const docId=codeModalDocId,fileType=codeModalFileType;
  const btn=document.getElementById('btnCodeOk');
  btn.disabled=true;
  btn.innerHTML='<span style="display:inline-block;width:13px;height:13px;border:2px solid rgba(255,255,255,.3);border-top-color:#fff;border-radius:50%;animation:spin .7s linear infinite;vertical-align:middle;margin-right:6px;"></span> Verificando…';
  try{
    const j=await fetch(`/api/print-queue/${docId}/validate-code`,{
      method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})
    }).then(r=>r.json());
    if(!j.success){
      shakeCode();
      toast(j.error||'Código incorrecto — intenta de nuevo','err',4000);
      btn.disabled=false;btn.innerHTML='<i class="bi bi-printer-fill"></i> 🖨️ Imprimir';
      return;
    }
    toast('✅ Código correcto — abriendo impresión…','ok',3000);
    closeCodeModal();
    launchPrint(docId,fileType,code);
  }catch(e){
    toast('Error de conexión: '+e.message,'err');
    btn.disabled=false;btn.innerHTML='<i class="bi bi-printer-fill"></i> 🖨️ Imprimir';
  }
}

function shakeCode(){
  const inp=document.getElementById('codeInput');
  inp.classList.remove('shake');void inp.offsetWidth;inp.classList.add('shake');
  setTimeout(()=>inp.classList.remove('shake'),500);
}

function launchPrint(id,fileType,code){
  const url=`/api/print-queue/download/${id}`;
  // Eliminar a los 2 minutos — el archivo ya está en el navegador mucho antes
  const scheduleCleanup=()=>{
    setTimeout(()=>{
      fetch(`/api/print-queue/${id}/confirm-printed`,{
        method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code})
      }).then(()=>{loadAll();if(activeRowId===id)closeDrawer();})
        .catch(e=>console.warn('confirm-printed:',e.message));
    },2*60*1000);
  }; 
  if(['pdf','image'].includes(fileType)){
    toast('Cargando archivo para imprimir…','inf',3000);
    fetch(url)
      .then(r=>{if(!r.ok)throw new Error('Archivo no disponible (cód '+r.status+')');return r.blob();})
      .then(blob=>{
        const blobUrl=URL.createObjectURL(blob);
        const win=window.open(blobUrl,'_blank');
        if(!win){
          toast('⚠️ Ventana bloqueada — permite pop-ups en la barra del navegador','inf',8000);
          const a=document.createElement('a');a.href=blobUrl;a.download='';a.click();
          setTimeout(()=>URL.revokeObjectURL(blobUrl),60000);
        }else{
          win.addEventListener('load',()=>{try{win.focus();win.print();}catch(e){}});
          setTimeout(()=>URL.revokeObjectURL(blobUrl),10*60*1000);
          toast('✅ PDF abierto — elige la impresora en la ventana','ok',5000);
        }
        scheduleCleanup();
      })
      .catch(err=>toast('❌ '+err.message,'err',6000));
  }else{
    window.open(url,'_blank');
    toast('Archivo abierto — imprime desde la aplicación nativa (Ctrl+P)','inf',5000);
    scheduleCleanup();
  }
}

// ── Eliminar del buzón ────────────────────────────────────
async function deleteMailbox(id){
  const btn=document.getElementById('mbBtn_'+id)||document.getElementById('mbBtnD_'+id);
  if(btn){btn.disabled=true;btn.innerHTML='<i class="bi bi-arrow-repeat spin-anim"></i> Eliminando…';}
  try{
    const j=await fetch(`/api/print-queue/${id}/mailbox`,{method:'DELETE'}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    toast('✅ Correo eliminado del buzón','ok');
    await loadAll();
    if(activeRowId===id){
      const rows=await fetch(`/api/print-queue/data?page=1&limit=200`).then(r=>r.json());
      const updated=(rows.data||[]).find(r=>r.id===id);
      if(updated)document.getElementById('drawerBody').innerHTML=buildDrawer(updated);
    }
  }catch(e){
    toast('Error: '+e.message,'err');
    if(btn){btn.disabled=false;btn.innerHTML='<i class="bi bi-envelope-x-fill"></i> Del buzón';}
  }
}

// ── Auto-sincronización cada 3 minutos ────────────────────
const AUTO_FETCH_INTERVAL=3;
let autoFetchTimer=null,autoCountdownTimer=null,autoFetchRunning=false;

function startAutoFetch(){
  stopAutoFetch();
  updateCountdownLabel(AUTO_FETCH_INTERVAL*60);
  autoFetchTimer=setInterval(async()=>{
    if(autoFetchRunning)return;
    autoFetchRunning=true;
    try{await fetchEmails(true);}catch(e){console.warn('Auto-fetch:',e.message);}
    finally{autoFetchRunning=false;}
  },AUTO_FETCH_INTERVAL*60*1000);
}
function stopAutoFetch(){clearInterval(autoFetchTimer);clearInterval(autoCountdownTimer);autoFetchTimer=null;autoCountdownTimer=null;}
function updateCountdownLabel(secondsLeft){
  clearInterval(autoCountdownTimer);
  let secs=secondsLeft;
  const update=()=>{
    const el=document.getElementById('autoFetchCountdown');if(!el)return;
    const m=Math.floor(secs/60),s=secs%60;
    el.textContent=secs<=0?'Sincronizando…':`Sync en ${m}:${String(s).padStart(2,'0')}`;
    if(secs>0)secs--;
  };
  update();autoCountdownTimer=setInterval(update,1000);
}

async function fetchEmails(silent=false){
  const btn=document.getElementById('btnFetch'),icon=document.getElementById('fetchIcon'),lbl=document.getElementById('fetchLabel');
  btn.disabled=true;icon.classList.add('spin-anim');lbl.textContent='Buscando...';
  if(!silent)toast('Conectando con Outlook…','inf',10000);
  try{
    const j=await fetch('/api/print-queue/fetch',{method:'POST'}).then(r=>r.json());
    if(!j.success)throw new Error(j.error||'Error desconocido');
    const vips=(j.log||[]).filter(l=>l.vip&&l.status==='encolado').length;
    const pages=(j.log||[]).filter(l=>l.pages).reduce((s,l)=>s+(l.pages||0),0);
    if(!silent||j.encolados>0)
      toast(`✅ ${j.emails_found} correos · ${j.encolados} nuevos${vips?` · ⭐ ${vips} VIP`:''}${pages?` · ${n(pages)} hojas`:''} · ${j.duplicados} dup.`,'ok',7000);
    await loadAll();
  }catch(e){
    if(!silent)toast('❌ '+e.message,'err',8000);
    else console.warn('Auto-fetch:',e.message);
  }finally{
    btn.disabled=false;icon.classList.remove('spin-anim');lbl.textContent='Buscar correos';
    updateCountdownLabel(AUTO_FETCH_INTERVAL*60);
  }
}

async function loadAll(){
  const icon=document.getElementById('refreshIcon'); 
  icon.classList.add('spin-anim');
  await Promise.all([loadData(currentPage),updateBadges()]);
  icon.classList.remove('spin-anim');
}

// ── VIP Manager ───────────────────────────────────────────
const openVipManager=async()=>{document.getElementById('vipModal').classList.add('open');await refreshVipList();};
const closeVipModal=()=>document.getElementById('vipModal').classList.remove('open');

async function refreshVipList(){
  const list=document.getElementById('vipList');
  try{
    const j=await fetch('/api/print-queue/vip-users').then(r=>r.json());
    if(!j.success||!j.data.length){list.innerHTML=`<div style="text-align:center;padding:20px;color:var(--muted);font-size:13px;"><i class="bi bi-star" style="font-size:28px;display:block;margin-bottom:8px;opacity:.3;"></i>Sin usuarios VIP aún</div>`;return;}
    list.innerHTML=j.data.map(u=>`
      <div class="vip-row">
        <div style="width:36px;height:36px;background:var(--gold-bg);border:1.5px solid rgba(245,158,11,.3);border-radius:50%;display:flex;align-items:center;justify-content:center;flex-shrink:0;"><i class="bi bi-star-fill" style="color:var(--gold);font-size:14px;"></i></div>
        <div class="vip-row-info"><div class="vip-row-name">${esc(u.name||'Sin nombre')}</div><div class="vip-row-email">${esc(u.email)}</div></div>
        <button class="btn-sm danger" onclick="removeVip(${u.id})"><i class="bi bi-star"></i> Quitar</button>
      </div>`).join('');
  }catch(e){list.innerHTML=`<div style="color:var(--danger);padding:12px;font-size:13px;">Error: ${e.message}</div>`;}
}

async function addVip(){
  const email=(document.getElementById('vipEmail').value||'').trim();
  const name=(document.getElementById('vipName').value||'').trim();
  if(!email){toast('Introduce un email','inf');return;}
  try{
    const j=await fetch('/api/print-queue/vip-users',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({email,name})}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    document.getElementById('vipEmail').value='';document.getElementById('vipName').value='';
    const docsMsg=j.docs_marked>0?` · ${j.docs_marked} doc(s) marcados`:' · sin documentos en cola aún';
    toast(`⭐ ${email} ahora es VIP${docsMsg}`,'ok',6000);
    await refreshVipList();await loadData(currentPage);
  }catch(e){toast('Error: '+e.message,'err');}
}

async function removeVip(id){
  try{
    const j=await fetch(`/api/print-queue/vip-users/${id}`,{method:'DELETE'}).then(r=>r.json());
    if(!j.success)throw new Error(j.error);
    toast('Usuario quitado de VIP','ok');await refreshVipList();await loadData(currentPage);
  }catch(e){toast('Error: '+e.message,'err');}
}

document.getElementById('confirmModal').addEventListener('click',function(e){if(e.target===this)closeConfirm();});
document.getElementById('vipModal').addEventListener('click',function(e){if(e.target===this)closeVipModal();});
document.getElementById('printedModal').addEventListener('click',function(e){if(e.target===this)this.classList.remove('open');});
document.getElementById('vipEmail')?.addEventListener('keydown',e=>{if(e.key==='Enter')addVip();});

(async()=>{
  await loadAll();
  startAutoFetch();
})();
</script>


<script>
/* ─── Dark Mode — init inmediato (sin flash) ────────── */
(function () {
    const saved = localStorage.getItem('dashboardTheme') || 'light';
    document.documentElement.setAttribute('data-theme', saved);
})();

function toggleTheme() {
    const html    = document.documentElement;
    const current = html.getAttribute('data-theme');
    const next    = current === 'dark' ? 'light' : 'dark';
    html.setAttribute('data-theme', next);
    localStorage.setItem('dashboardTheme', next);
    _syncToggleUI(next);
    if (typeof updateChartsTheme === 'function') updateChartsTheme(next);
}

function _syncToggleUI(theme) {
    const dark=theme==='dark';
    const icon=document.getElementById('toggleIcon'),label=document.getElementById('toggleLabel'),track=document.getElementById('themeTrack'),knob=document.getElementById('themeKnob');
    if(icon)icon.className=dark?'bi bi-sun-fill':'bi bi-moon-stars-fill';
    if(label)label.textContent=dark?'Claro':'Oscuro';
    if(track)track.style.background=dark?'#3b82f6':'#cbd5e1';
    if(knob)knob.style.transform=dark?'translateX(15px)':'translateX(0)';
}

document.addEventListener('DOMContentLoaded', function () {
    const saved = localStorage.getItem('dashboardTheme') || 'light';
    _syncToggleUI(saved);
});
window.addEventListener('message',function(e){
  if(e.data&&e.data.type==='theme') document.documentElement.setAttribute('data-theme',e.data.theme);
});
</script>
</body>
</html>
 